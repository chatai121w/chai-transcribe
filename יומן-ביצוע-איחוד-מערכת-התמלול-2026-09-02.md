# יומן ביצוע: איחוד מערכת התמלול והלמידה

תאריך פתיחה: 2 בספטמבר 2026
פרויקט: `chai-transcribe`
מטרה: לאחד מקורות סמכות כפולים בלי לערבב בין Raw, Corrected, Gold ו־Benchmark, ולהבטיח Trace ניתן לשחזור לכל שינוי טקסט.

## כללי עבודה מחייבים

1. לא מסירים מקור ישן לפני export, בדיקת migration ובדיקת import/route.
2. לא הופכים פלט של מנוע חיצוני ל־Gold ללא אישור אנושי.
3. לא מפעילים אימון אמיתי כאשר קיימים קטעים ללא איכות או מקור תווית.
4. כל שינוי טקסט דטרמיניסטי חייב להחזיר operation שניתן לשחזור.
5. כל מעבר AI מתועד כגבול אטום עם קלט ופלט, ואחריו שער צורני סופי.
6. תמלול Raw, תמלול Corrected, טקסט Gold ו־Benchmark נשמרים בנפרד.
7. כל שלב נסגר רק לאחר בדיקה חיובית, שלילית והפוכה מתאימה.

## מצב פתיחה מאומת

- בדיקות יחידה: 54 קבצים, 275 מתוך 275 עברו.
- TypeScript: אפס שגיאות.
- בניית Production: עברה עם אזהרות תחזוקה בלבד.
- Playwright של מעבדת התמלול: 10 מתוך 10 תרחישים עברו.
- בדיקות Python של Dataset Split ושער איכות: 14 מתוך 14 עברו.
- מודל `psalm_chant_ashkenazi_v1_fixed` אינו נטען: רישום המדדים אינו מלא, ו־CER החמיר גם לפי `progress.json`.
- Dataset תהילים: 99 קטעים משתי הקלטות מקור; כל 99 רשומות האיכות ומקור התווית מסווגות `unknown`.

## תוכנית וסטטוס

| שלב | תחום | סטטוס |
|---|---|---|
| 1 | שער איכות דאטה ו־metadata קנוני | הושלם ואומת |
| 2 | מקור אמת אחד לתיקונים כלליים ולפרופיל | הושלם ואומת מקומית ובענן |
| 3 | מימוש יחיד למנצפ״ך וכללים צורניים | הושלם ואומת |
| 4 | מקור יחיד ל־Prompt ול־Hotwords | הושלם ואומת |
| 5 | שקילות מלאה בין המעבדה למסלול הייצור | הושלם ואומת ב־unit, integration ו־Playwright |
| 6 | export, migration והסרת קוד רדום | הושלם כ־quarantine הפיך; מחיקה פיזית נדחתה בכוונה |
| 7 | בדיקות מלאות ודוח מסכם | הושלם; Publish של Lovable ממתין לחיבור מחדש של האינטגרציה |

## פעולות שבוצעו

### פעולה 1: קיבוע קו בסיס

נבדקו ה־working tree, תוצאות הבדיקות הקיימות, רישום המודל, manifests של האימון וההערכה, מפתח קבוצת ההקלטה ומדדי WER/CER/Term Recall. לא הופעל אימון חדש ולא שונה artifact של מודל.

ראיות מרכזיות:

- קיימת הפרדה של 76 קטעים מהקלטה אחת לאימון ו־23 מהקלטה אחרת להערכה.
- אין fingerprint היסטורי בתוך ריצת האימון הישנה, אך לוג הריצה מציין recording-group holdout.
- WER השתפר מ־80.30% ל־73.98%; CER החמיר מ־39.49% ל־41.38%.
- קיימים 53 מונחי יעד אפקטיביים, ואפס מהם מופיעים ב־reference של קבוצת ההערכה.

## פעולות הבאות

1. לאחד את כל כותבי ה־metadata דרך `buildApprovedAsrMetadata`.
2. להפוך `unknown` לחסם קשיח עבור אימון אמיתי.
3. להוסיף migration מפורש לרשומות ישנות; אין לשדרג אותן אוטומטית ל־Gold.
4. להוסיף בדיקות שרת ולקוח שמוכיחות את החסימה ואת שימור provenance.

### פעולה 2: איחוד metadata ושער איכות האימון

קבצים ששונו:

- `src/lib/asrDatasetMetadata.ts`
- `src/lib/asrDatasetMetadata.test.ts`
- `src/components/AudioLearningQueue.tsx`
- `src/components/training/ReferenceAudioImporter.tsx`
- `server/training_routes.py`
- `server/train_lora.py`
- `server/test_training_routes.py`

מה השתנה:

1. בונה ה־metadata הקנוני תומך מעתה ב־quality tier, מקור תווית, סטטוס בדיקה ותפקיד benchmark.
2. תור הלמידה מהעורך אינו כותב עוד שדות ישנים בלבד; הוא עובר דרך אותו builder כמו המעבדה.
3. יבוא אודיו עם טקסט מקור נשמר עם `labelSource=reference-audio-import` במקום להיראות כאישור אנושי רגיל.
4. השרת דוחה העלאת זוג שאינו כולל quality tier, מקור תווית ומזהה הקלטת מקור.
5. אימון אמיתי דורש לפחות 20 קטעי Gold, אפס Silver/Bronze/unknown, מקור תווית ידוע ולפחות שלוש הקלטות מקור, מהן לפחות שתיים באימון.
6. אימון אמיתי דרך manifest חופשי נחסם; יש להשתמש ב־dataset סופי עם provenance ו־recording-group holdout.
7. תהליך האימון מקבל `dataset_meta.json` ושומר את נתוני האיכות בתוך `progress.json`.
8. Model Registry שומר quality counts, label sources, קבוצות הקלטה ו־Term Recall, כך ששער ההפעלה יכול לבדוק את אותם נתונים.

בדיקות:

- `server.test_training_routes`: 15 מתוך 15 עברו.
- `src/lib/asrDatasetMetadata.test.ts`: 3 מתוך 3 עברו.
- הפעלה ישירה של finalize על Dataset התהילים החזירה `ready_for_training=false`.
- סיבות החסימה שנבדקו: שתי הקלטות מקור בלבד, 99 קטעים שאינם Gold ו־99 קטעים ללא provenance קנוני.

הגנת נתונים:

- אף רשומה ישנה לא שודרגה אוטומטית ל־Gold.
- קובצי האודיו, הטקסט וה־adapter הקיים לא נמחקו.
- המודל הישן נשאר artifact ניסויי ואינו הופעל.

### פעולה 3: מקור אמת אחד לתיקונים נלמדים

קבצים שנוספו או שונו:

- `src/lib/correctionRepository.ts`
- `src/lib/correctionRepository.test.ts`
- `src/utils/correctionLearning.ts`
- `src/lib/pronunciationProfiles.ts`
- `src/lib/personalPronunciationModel.ts`
- `src/components/PlayerTranscriptEditor.tsx`
- `src/hooks/useCorrectionLearning.ts`
- `src/lib/syncLearnedCorrections.ts`
- `src/integrations/supabase/types.ts`
- `supabase/migrations/20260902193000_unify_asr_correction_scope.sql`

מצב קודם שהוכח:

1. תיקונים כלליים נשמרו ב־`transcription_corrections`.
2. תיקוני פרופיל נשמרו בנפרד ב־`pp_profile_<id>_corrections`.
3. `verifyCorrection` כתב את אותו תיקון לשני המאגרים כאשר היה פרופיל פעיל.
4. `PlayerTranscriptEditor` כתב תחילה למאגר הכללי ואז שוב למאגר הפרופיל.
5. הסנכרון לענן הכיר רק את המאגר הכללי ולא כלל scope או profile.

מצב חדש:

1. כל התיקונים המקומיים נשמרים פיזית רק ב־`transcription_corrections_v2`.
2. לכל רשומה יש `scope=global|profile`; לרשומת פרופיל יש גם `profileId`.
3. אותו צמד מקור/תיקון בתוך אותו scope נשמר פעם אחת, גם אם הגיע משני מנועים.
4. פעולה אנושית כותבת ל־scope יחיד: לפרופיל הפעיל, או לגלובלי כאשר אין פרופיל.
5. Trace של תיקון מציין את המאגר הקנוני ואת ה־scope שממנו הופעל.
6. מאזיני שינוי בין טאבים מאזינים למפתח החדש; המפתח הישן נשאר רק לצורך migration.
7. סנכרון הענן משתמש בזהות `user + scope + profile + original + corrected`.
8. migration של Supabase משייך את כל הרשומות ההיסטוריות ל־global ומוסיף אילוץ שמונע פרופיל ריק או scope לא תקין.

הגירת נתונים והפיכות:

- בקריאה הראשונה, אם המאגר החדש עדיין לא קיים, כל התיקונים הכלליים וכל תיקוני הפרופילים מועתקים אליו.
- המפתחות הישנים אינם נמחקים ומשמשים גיבוי rollback בלבד.
- קיים marker עם מועד ההגירה, מספר הרשומות הכלליות, מספר רשומות הפרופיל ואישור שהמקור הישן נשמר.
- מיגרציית הענן נפרסה ונבדקה התנהגותית. ה־fallback לסכימה הישנה נשאר רק כתאימות לאחור ואינו הנתיב הרגיל בפרויקט הענן הנוכחי.

בדיקות שבוצעו:

- `correctionRepository.test.ts`: ארבע מתוך ארבע עברו.
- `correctionLearning.test.ts`: ארבע עשרה מתוך ארבע עשרה עברו.
- `transcriptionKnowledge.test.ts`: שש מתוך שש עברו.
- `transcriptionTrace.test.ts`: שמונה מתוך שמונה עברו.
- סך הכול בשלב: 32 מתוך 32 בדיקות עברו.
- `npx tsc --noEmit`: אפס שגיאות.

### פעולה 5: מקור גרסאי יחיד ל־Prompt ול־Hotwords

קבצים שנוספו או שונו:

- `shared/hebrew_transcription_defaults.json`
- `server/hebrew_transcription_defaults.py`
- `server/test_hebrew_transcription_defaults.py`
- `server/transcribe_server.py`
- `src/lib/loshonKodesh.ts`
- `src/lib/hebrewTranscriptionDefaults.test.ts`

מצב קודם שהוכח:

1. Prompt ברירת המחדל הוגדר בנפרד ב־TypeScript וב־Python, עם תוכן שונה.
2. רשימת מונחי לשון הקודש הוגדרה בנפרד בשני הצדדים, עם כיסוי שונה.
3. שינוי בצד אחד לא חייב שינוי בצד האחר ולכן זהות בקשת התמלול הייתה תלויה במסלול.

מצב חדש:

1. `shared/hebrew_transcription_defaults.json` הוא מקור ברירת המחדל היחיד והגרסאי.
2. לקוח TypeScript ושרת Python טוענים ממנו את אותו Prompt ואת אותן רשימות.
3. רשימת הבסיס היא איחוד ללא כפילויות של המונחים שהיו בשני הצדדים.
4. מונחים אישיים, מילון משתמש, תיקונים נלמדים ופרופיל דובר אינם הועברו ל־JSON; הם נשארו תוספות דינמיות ומדורגות לבקשה.
5. השרת ממשיך לבצע deduplication אחרי מיזוג הרשימה הדינמית עם ברירת המחדל.

בדיקות:

- בדיקות TypeScript: 18 מתוך 18 עברו.
- בדיקות Python למקור המשותף: שתיים מתוך שתיים עברו.
- נבדקה זהות מלאה בין exports של הלקוח לבין תוכן ה־JSON.
- נבדקה היעדרות כפילויות בכל אחת משתי רשימות ברירת המחדל.

### פעולה 6: יישור המעבדה למסלול הייצור המקומי

קבצים ששונו:

- `src/lib/transcriptionPipeline.ts`
- `src/lib/transcriptionPipeline.test.ts`

מצב קודם שהוכח:

1. מסך התמלול הראשי השתמש ב־`/transcribe-stream` של שרת CUDA.
2. מעבדת התמלול השתמשה ב־`/transcribe` הישן.
3. שני הנתיבים הגיעו לאותו שרת אך לא לאותו חוזה תגובה, ולכן ניסוי במעבדה לא היה העתק מדויק של נתיב הייצור.

מצב חדש:

1. גם המעבדה משתמשת ב־`/transcribe-stream`.
2. נוסף parser מפורש לחוזה SSE: אירועי source/info/loading/segment/done/error.
3. שגיאת stream אינה מוחזרת כהצלחה חלקית.
4. כאשר קיים אירוע done, הטקסט והתזמונים הסופיים שלו גוברים על accumulator חלקי.
5. לוג הגדרות הריצה ו־config snapshot מציינים `localTransport=transcribe-stream`.
6. לאחר קבלת Raw, המעבדה ומסך הייצור מפעילים את אותה `applyTranscriptionKnowledgeWithAi` ואת אותו `logKnowledgeTrace`.

בדיקות:

- בדיקות pipeline, audit ו־Trace: 15 מתוך 15 עברו.
- בדיקת SSE חיובית כוללת info, segment ו־done.
- בדיקת SSE שלילית מוכיחה שאירוע error מפיל את הריצה.
- `npx tsc --noEmit`: אפס שגיאות.

גבול ההכרעה:

- יישור זה חל על תמלול עברי מקומי במעבדה ובייצור.
- כלי עזר שאינם תמלול מסמך, כגון preload ובדיקת זמינות, נשארים endpoints נפרדים בכוונה.
- בדיקת דפדפן מלאה מול שרת פעיל תבוצע בשלב האימות הסופי.

### פעולה 7: גיבוי ובידוד מערכת SQLite הישנה

קבצים שנוספו או שונו:

- `shared/legacy_lk_sqlite_export_2026-09-02.json`
- `server/transcribe_server.py`
- `server/test_legacy_lk_api.py`

ממצאי מצב חי:

- `lk_dictionary`: אפס רשומות.
- `lk_sessions`: אפס רשומות.
- `lk_grammar_rules`: 14 רשומות, מהן 6 פעילות.
- ששת הכללים הפעילים קיימים גם ברשימת ההחלפות של המערכת הקנונית.
- שמונת הכללים האחרים כבויים; שישה מהם בכיוון המסוכן מתקין להגייה ושניים הם no-op.
- `/lashon-kodesh` ו־`/asr-training` כבר מפנים דרך הראוטר למעבדת התמלול.
- למרות זאת, `/lk/*` נשאר נגיש ישירות ולכן עדיין היה מסוגל ליצור מקור נתונים מקביל.

פעולות:

1. נוצר export מלא, קריא וגרסאי של שלוש הטבלאות ושל כל 14 הכללים.
2. ה־export הושווה ישירות למסד החי ונמצא זהה בכל הרשומות והספירות.
3. כל ממשקי `/lk/*` מחזירים מעתה HTTP 410 כברירת מחדל ומכוונים למסלול הקנוני.
4. rollback אפשרי רק בהפעלה מפורשת של `ENABLE_LEGACY_LK_API=1` לצורך שחזור מבוקר.
5. מסד SQLite וקוד המימוש לא נמחקו פיזית בשלב זה, כדי לשמור הפיכות; הם אינם מקור סמכות פעיל.
6. חיבורי SQLite נסגרים כעת במפורש גם במסלול rollback, לאחר שהבדיקה חשפה ResourceWarning של חיבור שלא נסגר.

בדיקות:

- ארבע מתוך ארבע בדיקות Python עברו עם `ResourceWarning` מוגדר כשגיאה.
- בדיקה שלילית הוכיחה כי `/lk/dictionary` חסום כברירת מחדל.
- בדיקה חיובית הוכיחה כי `/health` ו־`/transcribe-stream` עדיין רשומים ואינם נחסמים.
- בדיקת export מול `server/lk_data.db`: זהות מלאה וספירות 0/14/0.

החלטה לגבי מחיקה:

מחיקה פיזית של קבצי המסכים הישנים והקוד בשרת אינה נדרשת כדי למנוע דריסה, ועלולה למחוק עבודה מקומית קיימת. לכן בוצע quarantine ברירת־מחדל עם export ו־rollback. לאחר תקופת יציבות אפשר להסיר את הקוד בארכיון בשינוי נפרד.

### פעולה 8: אימות מקצה לקצה ותיקון חוזה בדיקות ה־SSE

מטרת הפעולה:

להוכיח שהאיחודים אינם רק נכונים בבדיקות יחידה, אלא עובדים גם דרך ממשק המעבדה, נתיב התמלול המקומי, שמירת ה־Trace, תיקון חוזר ו־Gold.

מה הורץ:

1. `npm run build` — build ייצור מלא.
2. `npx tsc --noEmit` — בדיקת טיפוסים מלאה.
3. `python -m unittest discover` עם `ResourceWarning` כשגיאה — בדיקות שרת ואימון.
4. `vitest run` — כל בדיקות היחידה של הלקוח.
5. `PLAYWRIGHT_SUITE=full` עם `transcription-lab.spec.ts` — דפדפן Chromium מבודד במצב headless.
6. `git diff --check` — בדיקת whitespace ושגיאות patch.

ממצא שנחשף ב־QA:

- הריצה הראשונה של Playwright הסתיימה ב־7 הצלחות ו־3 כישלונות.
- שלושת הכישלונות נעצרו בשלב התמלול ולא הגיעו ל־`experiment-completed`.
- בדיקה של snapshot, נתיב הרשת וקוד ה־fixture הוכיחה שכל שלושת המבחנים עדיין יירטו `**/whisper/transcribe` והחזירו JSON.
- הקוד הפעיל, אחרי איחוד המעבדה עם הייצור, פונה ל־`/transcribe-stream` ומפרש SSE.
- לכן הבעיה הייתה חוזה בדיקה מיושן, לא כשל מוסתר במנוע או ב־Trace.

התיקון:

- נוסף ל־`e2e/transcription-lab.spec.ts` builder יחיד בשם `transcriptionStream` שמחזיר אירוע `done` בפורמט SSE.
- שלושת ה־fixtures הועברו ליירוט `**/transcribe-stream` עם `content-type: text/event-stream`.
- גוף ה־multipart עדיין נאסף ונבדק, ולכן הבדיקה ממשיכה להוכיח שהאודיו המקורי והמעובד נשלחים כפי שנדרש.

תוצאות סופיות:

- Frontend unit: ‏57 קובצי בדיקה, 288 מתוך 288 עברו.
- Python: ‏30 מתוך 30 עברו, כולל דליפת recording groups, שער איכות, מקור Prompt משותף ובידוד `/lk/*`.
- Playwright מעבדה: ‏10 מתוך 10 עברו לאחר תיקון חוזה ה־fixture.
- TypeScript: אפס שגיאות.
- Production build: הושלם בהצלחה; 5,186 modules עברו transform.
- `git diff --check`: נקי. אזהרות LF/CRLF הן נרמול שורות צפוי ב־Windows ואינן שגיאת diff.

מה הוכח בדפדפן:

- ניסוי A/B מסתיים ונשמר.
- כל השלבים החומריים מופיעים ב־audit: מקור, הגדרות, העלאה, תמלול, ידע, מדדים, שער איכות, בדיקה אנושית והשלמה.
- ה־audit נשמר גם לאחר רענון.
- תיקון שאומת בידי אדם מופעל בריצה חוזרת של אותה הקלטה.
- Gold חלקי ו־Gold מלא נשמרים במסלולים המיועדים.
- אודיו קיים מהענן נטען בלי העלאה כפולה.
- עיבוד אודיו אופציונלי שומר fingerprint אחד ומבדיל בין original ל־processed.
- נתיבי לשון הקודש והאימון הישנים מפנים למעבדה הקנונית.
- תצוגת RTL נבדקה גם ב־viewport נייד ללא overflow אופקי.

אזהרות שאינן חוסמות:

- build מציג אזהרת chunk גדול עבור RNNoise ואזהרת import סטטי/דינמי של `audioCutEngine.ts`. אלה נושאי ביצועים וחלוקת bundle, לא כשל לוגי באיחוד.
- `caniuse-lite` אינו מעודכן בחצי השנה האחרונה. לא שונה dependency lock במסגרת איחוד התמלול.

### פעולה 9: מצב פריסה וגבולות ההשלמה

הושלם ונבדק בעץ העבודה המקומי:

- מקור תיקונים מקומי יחיד עם scope.
- מנצפ״ך במימוש יחיד ושער סופי אחרי AI.
- Prompt ומונחי ברירת מחדל ממקור משותף ל־TypeScript ול־Python.
- מעבדה וייצור מקומי באותו endpoint ובאותו parser.
- Trace מלא במעבדה ובפונקציית הידע המשותפת.
- בידוד ברירת־מחדל של ממשקי SQLite הישנים.
- שער metadata שמונע מנתוני `unknown` להיחשב מוכנים לאימון.

מצב חיצוני לאחר האיחוד:

- מיגרציית `20260902193000_unify_asr_correction_scope.sql` הוחלה בענן ונבדקה עם כתיבות global/profile, בדיקות constraints וניקוי מלא.
- הקוד נדחף לענף `main` ב־GitHub. ה־commit הראשון של חבילת האיחוד הוא `7a88a4f`.
- קבצי ה־legacy לא נמחקו פיזית; הם חסומים ומגובים, כדי לשמור rollback ולא למחוק עבודה קיימת.
- אתר Lovable הציבורי עדיין מגיש bundle ישן. פעולת Publish לא בוצעה דרך האינטגרציה משום שהחיבור ל־Lovable דורש reauthentication.

הכרעה מקצועית בנקודת זמן זו:

האיחוד הלוגי הושלם ונבדק מקומית. מצב הענן עודכן בפעולות 10–11 להלן. אין להתחיל ריצת LoRA חדשה לפני ששער הדאטה מדווח אפס רשומות `unknown` ומספר מספיק של recording groups נפרדים.

### פעולה 10: החלת מיגרציית האיחוד בענן Supabase

יעד:

- Repository: `chatai121w/chai-transcribe`.
- Supabase project: `pycryoyipkymaqorgpjy`, זהה ל־`supabase/config.toml` ול־runner הקנוני.
- Migration: `supabase/migrations/20260902193000_unify_asr_correction_scope.sql`.

בדיקת בטיחות לפני הרצה:

- אין `DROP TABLE`, ‏`TRUNCATE`, ‏`DELETE` או מחיקת תמלולים.
- קיימים שני `UPDATE` מצומצמים שמנרמלים רשומות ישנות ל־global ומרוקנים `profile_id` ברשומות global.
- אילוץ הייחוד הישן מוחלף באילוץ scoped; אין יצירת טבלה מקבילה.
- ה־runner בודק `migration_logs` ומונע הרצה כפולה ללא `--force`.

הרצה:

- בוצעה באמצעות `scripts/direct-run.mjs file` לפי `MIGRATION_RUNNER_GUIDE.md`.
- ההזדהות בוצעה כמשתמש המנהל הקיים, באמצעות משתני סביבה זמניים שנמחקו מיד מהתהליך.
- Edge Function `run-migration` החזירה success וזמן ביצוע של 326ms.

תוצאה:

- `scope` ו־`profile_id` קיימים כעת בענן.
- אילוץ ה־check ואילוץ ה־unique החדש נוצרו.
- המיגרציה נרשמה כהצלחה ב־migration runner.

### פעולה 11: בדיקת ענן התנהגותית וניקוי נתוני בדיקה

קובץ בדיקה שנוסף:

- `scripts/verify-correction-scope-cloud.mjs`.

מה הבדיקה עושה:

1. טוענת URL ומפתח ציבורי מ־`.env`, וסיסמת מנהל רק ממשתני סביבה.
2. מתחברת דרך Supabase Auth ללא session מתמשך.
3. מבצעת schema probe לעמודות `scope/profile_id`.
4. מוסיפה רשומת global זמנית ומוודאת שהערכים נשמרו בדיוק.
5. מנסה להוסיף כפילות זהה באותו scope ומחייבת שגיאת PostgreSQL `23505`.
6. מנסה profile ללא `profile_id` ומחייבת שגיאת check מסוג `23514`.
7. מוסיפה את אותו זוג source/target ב־profile תקין ומוכיחה ששני scopes נפרדים יכולים להתקיים בלי התנגשות.
8. קוראת את שתי הרשומות לפי המזהים המדויקים שלהן.
9. מוחקת רק את המזהים שנוצרו בבדיקה.
10. קוראת שוב ומחייבת אפס רשומות שנותרו.

תוצאת הריצה:

- schema columns readable: עבר.
- global persisted: עבר.
- profile persisted: עבר.
- duplicate rejected: עבר.
- invalid scope rejected: עבר.
- שתי רשומות מבודדות נקראו: עבר.
- cleanup verified: עבר; לא נותרו נתוני בדיקה.

גבול ההוכחה:

בדיקה זו מוכיחה את סכמת Supabase, ‏RLS של משתמש מחובר, אילוצי הזהות וניקוי הנתונים. היא אינה מוכיחה שה־frontend החדש כבר מופיע באתר Lovable הציבורי; לשם כך נדרשים Git push, Publish ובדיקת דפדפן נפרדת מול כתובת הייצור.

בדיקות שליליות והפוכות:

- החלפת הרשומות הגלובליות אינה מוחקת תיקוני פרופיל.
- החלפת תיקוני פרופיל אינה מוחקת את הרשומות הגלובליות.
- שני מנועים שמדווחים אותו תיקון אינם יוצרים שתי רשומות.
- migration אינו מוחק את מפתחות המקור הישנים.
- מחיקת פרופיל מסירה רק את ה־scope הפעיל שלו מן המאגר החדש; גיבוי ההגירה הישן נשמר.

### פעולה 4: איחוד מנצפ״ך לשער צורני יחיד

קבצים שנוספו או שונו:

- `src/lib/hebrewOrthography.ts`
- `src/lib/hebrewOrthography.test.ts`
- `src/lib/loshonKodesh.ts`
- `src/utils/hebrewRuleEngine.ts`
- `src/lib/transcriptionKnowledge.ts`

מצב קודם שהוכח:

1. טבלאות ההמרה ומימוש שינוי האותיות הופיעו גם ב־`loshonKodesh.ts` וגם ב־`hebrewRuleEngine.ts`.
2. `transcriptionKnowledge` הפעיל שער התחלתי, שער סופי ושער אחרי AI, אך ייחס את מקור הכלל לקובץ לשון הקודש ולא לרכיב צורני עצמאי.
3. מנוע הכללים הדטרמיניסטי הפעיל שוב לוגיקת מנצפ״ך משלו בתוך אותו צינור.

מצב חדש:

1. `hebrewOrthography.ts` הוא המימוש היחיד שמשנה צורות כ/מ/נ/פ/צ ו־ך/ם/ן/ף/ץ.
2. `loshonKodesh.ts` שומר export תואם לאחור, אך רק מעביר את הקריאה למימוש הקנוני.
3. `hebrewRuleEngine.ts` משתמש במפות ובפונקציית הנרמול הקנוניות; אין בו עוד טבלאות המרה או לולאת שינוי עצמאית.
4. Trace מציין כעת במפורש את `src/lib/hebrewOrthography.ts`, את הפונקציה הקנונית ואת המפה שהפעילה את השינוי.
5. השער מופעל לפני כללי הלמידה לצמצום רעש, אחרי שחזור תיקונים, ושוב אחרי AI כדי להבטיח שהתוצר האחרון תקין.

בדיקות חיוביות, שליליות והפוכות:

- כל חמש האותיות הרגילות הופכות לסופיות בסוף מילה.
- כל חמש האותיות הסופיות חוזרות לרגילות בתחילת או באמצע מילה.
- ניקוד, טעמים, פיסוק ומקף עברי נשמרים.
- קיצורים עבריים אינם נשברים.
- הפעלה שנייה על טקסט תקין מחזירה אפס שינויים.
- AI שמחזיר בכוונה `שלומ םשה` מתועד כגבול AI אטום, ואז השער האחרון מחזיר `שלום משה`.
- הפעלת מערכות הכללים האופציונליות במצב כבוי אינה מכבה את שער האורתוגרפיה המחייב.

בדיקות שבוצעו:

- שש חבילות ממוקדות: 52 מתוך 52 עברו.
- `hebrewRuleEngine.test.ts`: ארבע מתוך ארבע עברו.
- `npx tsc --noEmit`: אפס שגיאות.

### פעולה 12: דחיפה ל־GitHub ואימות CI

דחיפה ראשונה:

- Branch: `main`.
- Commit: `7a88a4ff6c9dcfcbe921d46201e65cf40f225c7e`.
- Message: `Unify Hebrew transcription learning pipeline`.
- הועלו רק 62 קבצי המימוש, הבדיקות, המיגרציה והדוחות של האיחוד. מסמכי Markdown לא קשורים שנמצאו כ־untracked לא נכללו ולא נמחקו.

תוצאת GitHub Actions לריצה `33668319272`:

- job בשם `build`: עבר. הוא כלל התקנת dependencies, בדיקת TypeScript, שער איכות ASR, guard נגד degeneration ובניית Vite.
- job בשם `e2e`: 64 בדיקות עברו, 4 דולגו ובדיקה אחת נכשלה.
- הכשל לא היה שינוי התנהגות: `getByText` מצא גם את כרטיס ה־Toast וגם proxy נגישות עם `role=status`, ולכן Playwright עצר ב־strict mode.
- ה־assertions עודכנו להתאמה מלאה ומדויקת של הכותרת והתיאור, בלי שינוי בקוד המוצר.
- אימות מקומי לאחר התיקון: `e2e/transcription.spec.ts` עבר 17 מתוך 17 בשני workers.
- התיקון נדחף ב־commit `438f947614b146f7a4331a7038082b9c7c3abca7`.
- ריצת CI חוזרת `33670430977` הסתיימה בהצלחה: job ה־build עבר וכל 65 בדיקות ה־E2E הרגילות עברו; 4 בדיקות אינטגרציה אמיתיות שמחייבות שירותי מדיה חיצוניים דולגו לפי הגדרת ה־suite.

בדיקת עומס מקומית נפרדת:

- בהרצת כל suite מקומית עם שמונה workers התקבלו שמונה timeouts מוקדמים בזמן שעמודים ראשונים נטענו באיטיות חריגה.
- אותם תרחישי auth/transcription הורצו בסדרה; כל 39 תרחישי ה־UI והמוקים הרלוונטיים עברו.
- benchmark האודיו האמיתי נעצר בשער הבריאות מפני שהשרת המקומי דיווח `model_ready=false`; עשר בדיקות שתלויות במודל לא רצו. זו מגבלת סביבת CUDA הנוכחית, לא הוכחת תקלה בענן או ב־frontend.

### פעולה 13: אימות שירותי ענן וגרסת האתר הציבורי

Supabase ו־Edge Functions:

- `gemini-health` החזירה HTTP 200, ‏`overall=ok`, בדיקת Lovable AI עברה ובדיקת פורמט האודיו עברה.
- `transcribe-gemini` החזירה HTTP 200 לבקשת CORS preflight עם `Access-Control-Allow-Origin: *`.
- מיגרציית scope והתנהגות RLS/constraints נבדקו בפעולה 11.

Lovable production:

- `https://chai-transcribe.lovable.app/` מחזיר HTTP 200.
- ה־bundle המקומי החדש היה `index-DEfkJ3t3.js`, בעוד האתר הציבורי הגיש `index-BMXoGVpe.js` יחד עם `~flock.js`.
- לכן האתר זמין, אך אינו מריץ עדיין את ה־commit החדש. זו ראיה ישירה לכך ש־Publish לא הושלם.
- ניסיון לקרוא ולפרוס את פרויקט Lovable דרך החיבור המובנה נחסם בהודעה שהחיבור דורש reauthentication. לא בוצע מעקף ולא נטענה הצלחת פריסה.

### פעולה 14: תנאי הסגירה שנותר

לאחר חיבור מחדש של Lovable יש לבצע, לפי הסדר:

1. לאמת שהפרויקט `e7bac46b-d538-45e4-b8dc-a31718ffa052` מסונכרן ל־`main` ול־commit האחרון.
2. לבצע Publish/Deploy מפורש.
3. לקרוא מחדש את HTML הייצור ולוודא שה־asset hash השתנה מה־bundle הישן.
4. להריץ Playwright headless על עמוד הבית, `/transcribe`, `/text-editor` ו־`/transcription-lab`.
5. לאסוף console errors, ‏page errors ובקשות רשת שנכשלו; אין לסמן את שלב הייצור כתקין לפני שכל הבדיקות עוברות.
