# מיפוי מלא של מערכות התמלול, הלמידה והכללים

תאריך בדיקה: 2 בספטמבר 2026
פרויקט: `chai-transcribe`
מטרת המסמך: לתאר במדויק מה קיים כיום, מאיזה עמוד וקובץ כל מסלול מתחיל, באיזה סדר הטקסט משתנה, היכן נשמר הידע, היכן יש כפילות, ומה נכון לאחד או להשאיר נפרד.

## 1. מסקנה מנהלית

הפרויקט **לא נהרס ולא צריך להתחיל מחדש**. יש בסיס עובד ובנוי היטב למדי, אבל התפתחו סביבו כמה מסלולים ומאגרי ידע בזמנים שונים. לכן קשה היה לענות על השאלה הפשוטה "מי שינה את המילה הזאת?".

התמונה הנכונה כיום היא:

1. יש **צינור עיבוד טקסט מרכזי פעיל אחד**: `applyTranscriptionKnowledgeWithAi`.
2. מסך התמלול הראשי, התמלול החוזר ומעבדת התמלול משתמשים בצינור הזה.
3. נוספה שרשרת `Trace` מדויקת שמסוגלת לשחזר כל שינוי דטרמיניסטי ולציין מערכת, קובץ, פונקציה, כלל ומיקום בטקסט.
4. מסלולי Benchmark ושיפור אודיו עוקפים בכוונה את התיקונים בחלק מהריצות, כדי למדוד תמלול גולמי. זו **הפרדה נכונה**, לא כפילות.
5. קיימת מערכת לשון הקודש ישנה ב־Python/SQLite, אך הנתיבים אל המסך הישן הוחלפו בהפניה למעבדה. זו **כפילות רדומה**.
6. קיימים שישה כללי SQLite פעילים, וכולם כפולים אחד לאחד בכללי הדפדפן.
7. תיקון אנושי יכול להיכתב גם למאגר הכללי וגם לפרופיל הפעיל. זו **כפילות פעילה של סמכות**, גם אם תוצאת ההחלפה לעיתים זהה.
8. מנצפ״ך נאכף ביותר ממקום אחד. שער סופי הוא חובה; האכיפה המקבילה בתוך `DEFINITIVE_RULES` צריכה בעתיד להפוך לאותו מימוש משותף או לבדיקת אבחון בלבד.
9. אין כיום הוכחה שכל כלי עזר באתר שומר `Trace`; יש Trace מלא במסלולי הייצור המרכזיים ובמעבדה, ואילו כלי דיבור/בדיקה עצמאיים נשארו גולמיים או מבודדים.

המלצת העל: **לאחד סמכות, לא לאחד מטרות**. מנוע ייצור, מעבדת מדידה, איסוף Gold וכלי עזר צריכים להישאר תפקידים נפרדים, אך כולם צריכים לקרוא לאותם שירותי תמלול, אותם מקורות ידע ואותו פורמט תוצאה.

## 2. איך בוצעה הבדיקה

הבדיקה כללה:

- סריקת כל נתיבי React הפעילים ב־`src/App.tsx`.
- איתור כל קריאה ל־`/transcribe`, ‏`/transcribe-stream`, ‏`/transcribe-live`, ‏`/lk/transcribe` ולכל Edge Function של תמלול.
- איתור כל קריאה לצינור הידע ולפונקציות הלמידה.
- מיפוי מפתחות `localStorage`, טבלאות Supabase וטבלאות SQLite.
- קריאה ישירה של מסד `server/lk_data.db`.
- השוואה מכנית בין כללי SQLite הפעילים ובין כללי ברירת המחדל בדפדפן.
- בדיקות חיוביות, שליליות, הפוכות ואדוורסריות ל־Trace.
- בדיקות יחידה מלאות, TypeScript, בניית Production ו־Playwright headless.

הערה חשובה: סכמת Supabase מופתה מקובצי המיגרציה ומהקריאות בקוד. במסמך זה לא נטען שמספר השורות הנוכחי בכל טבלת Supabase אומת מול הענן. לעומת זאת, ספירת טבלאות וכללי SQLite אומתה ישירות מול הקובץ המקומי.

### 2.1 תוצאות האימות הסופיות

| שכבת אימות | תוצאה | מה היא מוכיחה | מה היא אינה מוכיחה |
|---|---:|---|---|
| בדיקות יחידה | 54 קבצים, 275 מתוך 275 בדיקות עברו | מנגנוני הכללים, ה־Trace, האחסון וההתנהגויות היחידתיות עומדים באורקלים הנוכחיים | לא מוכיח שספק ענן חיצוני מחזיר תמלול איכותי בזמן אמת |
| TypeScript | 0 שגיאות | כל החיבורים והטיפוסים החדשים עקביים בזמן קומפילציה | לא מוכיח התנהגות דפדפן או רשת |
| בניית Production | עברה | חבילת היישום נבנית בהצלחה | לא מוכיח שהפריסה החיה עודכנה |
| Playwright headless של המעבדה | 10 מתוך 10 תרחישים עברו | A/B, שמירת Audit אחרי רענון, Gold, הקלטה קיימת, תיקון מילון, עיבוד אודיו, הקלטת מושגים, מעבר מהעורך ו־RTL mobile עובדים בתרחישים המבוקרים | התשובות של מנועי הענן דומו בבדיקה; זו אינה בדיקת מכסה, מפתח או איכות ספק חי |
| בדיקות Trace ממוקדות | 11 מתוך 11 עברו | שחזור פעולות, זיהוי שרשרת שבורה, שינוי לא מתועד, סתירה, idempotence וכשל אחסון מכוסים | Trace אינו חושף את reasoning הפנימי של מודל AI |

אזהרות הבנייה שנותרו אינן כשל פונקציונלי: נתוני Browserslist ישנים, מודול עיבוד אודיו שמיובא גם דינמית וגם סטטית, וחבילות JavaScript גדולות מן הסף המומלץ. יש לטפל בהן כעבודת ביצועים ותחזוקה נפרדת, לא כראיה לדריסת טקסט.

## 3. ארבעת התפקידים שצריך לשמור נפרדים

| תפקיד | מה הוא עושה | האם משנה טקסט | האם צריך Trace | החלטה |
|---|---|---:|---:|---|
| תמלול ייצור | מפיק תמלול למשתמש ושומר אותו | כן | כן, תמיד | צינור אחד מרכזי |
| ניסוי ומדידה | משווה בסיס מול מועמד ומחשב WER/CER/Term Recall | לפעמים; בסיס חייב להישאר גולמי | כן | להשאיר כמעבדה נפרדת |
| איסוף למידה | Human Review, Gold, מילון, דוגמאות אימון | משנה מאגר ידע, לא את האודיו | כן | מקור אמת אחד לכל סוג נתון |
| כלי עזר | פקודות קוליות, שדה דיבור, בדיקת פרופיל, שיפור אודיו | תלוי בכלי | Trace קל או סימון "raw" | לא לחבר בכוח לצינור הייצור |

## 4. מפת הנתיבים הפעילים באתר

כל הנתיבים מוגדרים ב־`src/App.tsx:274-303`.

| כתובת | רכיב פעיל | תפקיד | מצב |
|---|---|---|---|
| `/transcribe` | `src/pages/Index.tsx` | תמלול ייצור | פעיל |
| `/text-editor` | `src/pages/TextEditor.tsx` | עריכה, גרסאות ולמידה | פעיל |
| `/transcription-lab` | `src/pages/TranscriptionLab.tsx` | A/B, מדדים, Human Review, Gold | פעיל |
| `/lashon-kodesh` | הפניה אל `/transcription-lab?mode=lashon-kodesh` | תאימות לכתובת ישנה | פעיל כהפניה בלבד |
| `/asr-training` | הפניה אל `/transcription-lab` | תאימות למסך אימון ישן | פעיל כהפניה בלבד |
| `/loshon-kodesh-rules` | הפניה אל `/personal-learning?tab=loshon-kodesh` | ניהול כללי לשון הקודש | פעיל כהפניה |
| `/personal-learning` | `src/pages/PersonalLearning.tsx` | ניהול ידע אישי ומילון | פעיל |
| `/compare` | `src/pages/ComparisonsHub.tsx` | השוואות, מגמות, שיפור אודיו ודוברים | פעיל |
| `/benchmark` | הפניה אל `/compare?tab=enhance` | תאימות | פעיל כהפניה |
| `/youtube` | `src/pages/YouTube.tsx` | הורדה/תמלול ופתיחה בעורך | פעיל |
| `/voice-studio` | `src/pages/VoiceStudio.tsx` | הכנת אודיו ושליחה לתמלול | פעיל |
| `/audio-clean` | `src/pages/AudioCleanLab.tsx` | עיבוד אודיו | פעיל |
| `/voice-command-admin` | `src/pages/VoiceCommandAdmin.tsx` | פקודות קוליות | פעיל ומבודד |

### קבצים ישנים שאינם עמוד פעיל

| קובץ | מה נשאר בו | ראיה שאינו נתיב פעיל | החלטה |
|---|---|---|---|
| `src/pages/LashoKodesh.tsx` | מסך מלא, `/lk/transcribe`, מילון SQLite, סשנים ו־Benchmark | `/lashon-kodesh` מפנה למעבדה; אין import פעיל של העמוד | לגבות ואז למחוק/לארכב |
| `src/pages/AsrTraining.tsx` | מנועי A/B, pending corrections, sync ולמידה | `/asr-training` מפנה למעבדה; אין רכיב Route פעיל | להעביר רק יכולת חסרה ואז להסיר |

אסור להסיק מכך ששני הקבצים האלה דורסים היום תמלול רגיל. הם קוד רדום. הסיכון שלהם הוא תחזוקתי: מפתח עלול לתקן מערכת לא פעילה או להחזיר בטעות מסלול כפול בעתיד.

## 5. מסלול הייצור הראשי: `/transcribe`

### 5.1 נקודת כניסה

- עמוד: `src/pages/Index.tsx`.
- פונקציית שמירה מרכזית: `saveToHistory`, שורה 442.
- הפעלת צינור הידע: `applyTranscriptionKnowledgeWithAi`, שורה 446.
- שמירת Trace: `logKnowledgeTrace`, שורה 462, עם `surface: transcribe`.

### 5.2 מנועי תמלול שנכנסים לאותה נקודת שמירה

| מנוע | פונקציה בעמוד | נקודת ספק/שרת | חזרה ל־`saveToHistory` |
|---|---|---|---:|
| OpenAI Whisper | `Index.tsx:789` | Edge `transcribe-openai` בשורה 825 | כן, שורה 861 |
| Groq Whisper | `Index.tsx:906` | Edge `transcribe-groq` בשורה 945 | כן, שורה 995 |
| Google STT | `Index.tsx:1040` | Edge `transcribe-google` בשורה 1087 | כן, שורה 1133 |
| Whisper בדפדפן | `Index.tsx:1177` | מודל מקומי בדפדפן | כן, שורה 1182 |
| CUDA מקומי | `Index.tsx:1256` | `useLocalServer`, בדרך כלל `/transcribe-stream` | כן, שורות 1381-1385 |
| AssemblyAI | `Index.tsx:1459` | Edge `transcribe-assemblyai` בשורה 1492 | כן, שורה 1524 |
| Deepgram | `Index.tsx:1567` | Edge `transcribe-deepgram` בשורה 1600 | כן, שורה 1632 |
| Gemini | `Index.tsx:1675` | Edge `transcribe-gemini` בשורה 1723 | כן, שורה 1759 |

מסקנה: מבחינת **עיבוד הטקסט אחרי שהמנוע החזיר תוצאה**, כל מנועי התמלול הראשיים מתנקזים לאותה פונקציה.

### 5.3 תמלול חי

- רכיב: `src/components/LiveTranscriber.tsx`.
- CUDA חי: `/transcribe-live` בשורות 443 ו־569.
- ספקי ענן חיים: Google, Groq ו־Gemini מתוך אותו רכיב.
- סיום הריצה חוזר ל־`Index.tsx:2621` דרך `onTranscriptComplete`.
- `Index` קורא ל־`saveToHistory` בשורה 2641.

מסקנה: הצ'אנקים החיים עצמם גולמיים, אך התוצאה הסופית שנשמרת עוברת בצינור הידע וב־Trace של הייצור.

## 6. מסלול תמלול חוזר מתוך העורך

- דיאלוג: `src/components/RetranscribeDialog.tsx:62`.
- מנועי ענן מאוחדים דרך `src/lib/retranscriptionRunner.ts:141`.
- CUDA מקומי משתמש בשרת המקומי.
- לאחר קבלת הטקסט: `applyTranscriptionKnowledgeWithAi` בשורה 304.
- Trace: `logKnowledgeTrace` בשורה 308, עם `surface: retranscribe-dialog`.
- שמירת גרסה: callback אל `src/pages/TextEditor.tsx:1433`, ומשם `saveCloudVersion`.
- יעד ענן: `transcript_versions`; מצב העבודה נשמר גם כגרסת השוואה.

מסקנה: זה מסלול ייצור תקין ונפרד ברמת UI, אך לא מנוע תיקונים נפרד. הוא משתמש בצינור המרכזי.

## 7. מעבדת התמלול: `/transcription-lab`

### 7.1 נקודות קוד

- עמוד: `src/pages/TranscriptionLab.tsx:152`.
- התחלת ניסוי: `runExperiment`, שורה 376.
- ריצת בסיס A: `runTranscriptionPipeline`, שורה 478.
- ריצת מועמד B: `runTranscriptionPipeline`, שורה 504.
- הצינור המשותף: `src/lib/transcriptionPipeline.ts:137`.
- תמלול CUDA במעבדה: `src/lib/transcriptionPipeline.ts:107`, endpoint ‏`/transcribe`.
- Trace מרכזי: `src/lib/transcriptionPipeline.ts:217`.
- Human Review: `TranscriptionLab.tsx:759`.
- אישור Gold מלא: `TranscriptionLab.tsx:669`.

### 7.2 משמעות A ו־B

- ריצת A אמורה לשמש בסיס. כשהיא מוגדרת `useKnowledge: false`, היא מדלגת על כל צינור הידע ושומרת אירוע `knowledge-trace-skipped`.
- ריצת B מפעילה את הידע, המילון, פרופיל ההגייה, לשון הקודש, AI לפי בחירה והשערים הסופיים.
- לשתי הריצות מחושב fingerprint של אותו קובץ מקור.
- המדדים מחושבים מול טקסט אמת: WER, CER, גרסאות אורתוגרפיות, Term Recall ויחס אורך.
- שיפור אודיו אופציונלי הוא ניסוי נפרד: אותו fingerprint, אבל A מקבל מקור ו־B מקבל אודיו מעובד.

### 7.3 למה המעבדה לא זהה לגמרי לייצור CUDA

המעבדה קוראת ל־`/transcribe`, בעוד שמסלול הייצור הארוך משתמש בדרך כלל ב־`/transcribe-stream`. שני ה־endpoints משתמשים באותו שרת ומודל, אך הגדרות streaming, חלוקה למקטעים והקשר קודם עלולים להשפיע על הפלט. לכן:

- המעבדה טובה למדידת כללי טקסט, מילון, AI ושיפור אודיו.
- כדי להוכיח שקילות מלאה לייצור CUDA, צריך להוסיף למעבדה מצב `production-stream` או שירות תמלול משותף שמסתיר את ההבדל.

## 8. שרשרת הידע המרכזית, שלב אחר שלב

מימוש: `src/lib/transcriptionKnowledge.ts:213` ו־`:329`.

| # | מזהה Trace | מה קורה | קובץ ופונקציה | מקור נתונים | סמכות |
|---:|---|---|---|---|---|
| 1 | `orthography-pre` | מנצפ״ך מוקדם כדי לתת קלט תקין לכללים | `src/lib/loshonKodesh.ts:475`, `normalizeHebrewFinalLettersDetailed` | hardcoded | מסייע |
| 2 | `learned-corrections` | תיקונים שנלמדו מעריכות | `src/utils/correctionLearning.ts:310`, `applyLearnedCorrections` | `localStorage:transcription_corrections` | אנושי/הסתברותי |
| 3 | `learned-protection` | מחליף פלטים שנלמדו בטוקנים פרטיים | `src/lib/transcriptionKnowledge.ts:139`, `protectLearnedOutputs` | runtime בלבד | מניעת דריסה |
| 4 | `loshon-kodesh` | כללי הגייה ומונחים | `src/lib/loshonKodesh.ts:593` | defaults + `lk_rules_replacements` + מילונים | דטרמיניסטי מותנה |
| 5 | `vocabulary` | תיקוני מילון מאומת בלבד | `src/utils/customVocabulary.ts:441` | `custom_vocabulary`; בענן `torah_lexicon_terms` | אנושי מאומת |
| 6 | `pronunciation-profile` | תיקונים ששויכו לדובר/פרופיל | `src/lib/pronunciationProfiles.ts:434` | `pp_profile_<id>_corrections` | פרופיל |
| 7 | `definitive-rules` | רווחים וכללי עברית מוחלטים | `src/utils/hebrewRuleEngine.ts:302` | `DEFINITIVE_RULES` hardcoded | כלל ברזל |
| 8 | `learned-restore` | מחזיר את התיקונים המוגנים | `src/lib/transcriptionKnowledge.ts:139` | runtime | מונע דריסה |
| 9 | `orthography-final` | שער מנצפ״ך סופי אחרי כל הכללים | `src/lib/loshonKodesh.ts:475` | hardcoded | כלל ברזל |
| 10 | `ai-context-fix` | תיקון הקשר באמצעות AI | `src/lib/loshonKodesh.ts:723`, `applyLkAiFix` | Edge `loshon-kodesh-ai` | לא דטרמיניסטי |
| 11 | `orthography-after-ai` | מנצפ״ך סופי אחרי תשובת AI | `src/lib/loshonKodesh.ts:475` | hardcoded | כלל ברזל אחרון |

### סדר הקדימויות בפועל

1. תיקון אנושי שנלמד מופעל מוקדם.
2. הפלט שלו מוגן כדי שכללי לשון הקודש, מילון, פרופיל וכללים מוחלטים לא יחליפו אותו.
3. לאחר השחזור מופעל שער מנצפ״ך.
4. אם AI פעיל, ה־AI עדיין יכול לשנות משפט שלם אחרי כל השלבים הדטרמיניסטיים.
5. לאחר AI מופעל שוב רק שער מנצפ״ך.

לכן התשובה לשאלה "האם הכללים דורסים זה את זה?" היא:

- תיקון נלמד מוגן מפני הכללים הדטרמיניסטיים שאחריו.
- AI יכול לשנות אותו, משום שה־AI הוא השלב המאוחר ביותר מבחינת משמעות.
- מנצפ״ך יכול לשנות את צורת האות האחרונה אחרי AI, אבל לא לבחור מילה אחרת.
- אם AI שינה מילה נכונה למילה אחרת, ה־Trace יראה במדויק את הקלט והפלט של שלב AI, אך לא יכול לחשוף את תהליך החשיבה הפנימי של המודל.

## 9. מה ה־Trace שומר בדיוק

מימוש בסיסי: `src/lib/transcriptionTrace.ts`.

- checksum ואורך לפני ואחרי כל שלב.
- מזהה מערכת.
- נתיב קובץ מקור.
- שם פונקציה.
- שם מאגר הנתונים.
- מזהה כלל.
- מספר סידורי של פעולה.
- לכל מופע: טקסט לפני, טקסט אחרי, offset בקלט ו־offset בפלט.
- סטטוס: `applied`, ‏`no-change`, ‏`skipped`, ‏`error`.
- סיבת דילוג או שגיאה.
- חפיפה שהתממשה בזמן ריצה: כפילות או סתירה.
- אימות שניתן לשחזר את הפלט מן הקלט ומרשימת הפעולות בלבד.

פונקציות הבקרה:

- `checksumText` בשורה 89.
- `replayTraceOperation` בשורה 98.
- `createTraceOperation` בשורה 109.
- `createTraceStage` בשורה 130.
- `validateTraceChain` בשורה 174.
- `findTraceOverlaps` בשורה 187.

שמירת היומן מאוחדת ב־`src/lib/pipelineAudit.ts:117`, ‏`logKnowledgeTrace`.

יעדים:

- מקומי: `localStorage:asr_pipeline_events_v1`.
- ענן: טבלת `asr_pipeline_events` כאשר יש משתמש מחובר.
- UI: אירוע `asr-pipeline-event`, שמרענן את יומן המעבדה.

הגנת יציבות: אם `localStorage` מלא או חסום, שמירת התמלול אינה נכשלת. נשמרת רשומת אבחון מצומצמת ככל האפשר, ה־event החי ממשיך, וניסיון השמירה לענן ממשיך.

## 10. מקורות הידע ומקומות השמירה

### 10.1 תיקונים נלמדים כלליים

- כתיבה: `src/utils/correctionLearning.ts:249`, ‏`learnFromCorrections`.
- קריאה/החלה: `src/utils/correctionLearning.ts:310`, ‏`applyLearnedCorrections`.
- אחסון מקומי: `transcription_corrections`.
- סנכרון ענן: `src/lib/syncLearnedCorrections.ts`, טבלת `asr_learned_corrections`.
- מקור מרכזי לעריכות: `src/hooks/useCorrectionLearning.ts:17`.
- קריאות נוספות קיימות ב־`RichTextEditor`, ‏`SyncMirrorLayout`, ‏`PlayerTranscriptEditor` ו־`SpellCheckView`.

סיכון: לא כל עריכה היא Gold. החלפת מילה ממוקדת יכולה להיות תיקון ASR; ניסוח מחדש של משפט אינו בהכרח דוגמת אימון קולי.

### 10.2 מילון מונחים מרכזי

- מימוש: `src/utils/customVocabulary.ts`.
- אחסון: `localStorage:custom_vocabulary`.
- החלה: רק רשומות `verified` עם confidence של לפחות 0.85, בשורה 441 ואילך.
- סנכרון ענן: `src/lib/torahLexiconCloud.ts`, טבלת `torah_lexicon_terms`.
- Hook פעיל: `src/hooks/useCustomVocabulary.ts`.
- דירוג hotwords לפי הקשר: `src/utils/customVocabulary.ts:328`.

זה צריך להיות מקור האמת למונחים קנוניים, וריאציות הגייה, קטגוריה, הקשר ומעמד אישור.

### 10.3 פרופילי הגייה

- אינדקס: `pp_profiles_index`.
- פרופיל פעיל: `pp_active_profile`.
- תיקונים: `pp_profile_<id>_corrections`.
- החלה: `src/lib/pronunciationProfiles.ts:434`.
- הוספה: `src/lib/pronunciationProfiles.ts:228`.

בעיה מוכחת: `src/lib/personalPronunciationModel.ts:144-167` כותב תיקון מאומת למאגר הכללי וגם לפרופיל הפעיל. גם `PlayerTranscriptEditor` עושה דפוס דומה. עדיף לשמור correction אחד עם `scope = global | profile` ו־`profileId`, ולא שתי רשומות עצמאיות.

### 10.4 כללי לשון הקודש בדפדפן

- 46 hotwords בסיסיים.
- 56 כללי החלפה בסיסיים.
- 6 מילונים נושאיים; 5 פעילים כברירת מחדל.
- כל ששת המילונים מכילים hotwords בלבד ו־0 replacements.
- מפתחות: `loshon_kodesh_mode`, ‏`lk_rules_prompt`, ‏`lk_rules_hotwords`, ‏`lk_rules_replacements`, ‏`lk_rules_postprocess`, ‏`lk_rules_categories`, ‏`lk_rules_dictionaries`, והגדרות AI.

ה־hotwords אינם תיקון טקסט. הם הטיה הסתברותית של המפענח. כללי replacement כן משנים טקסט לאחר התמלול.

### 10.5 כללי עברית מוחלטים

- קובץ: `src/utils/hebrewRuleEngine.ts`.
- כללים: מנצפ״ך בסוף, ביטול אות סופית באמצע וריווח/פיסוק.
- שער מנצפ״ך נוסף ממומש ב־`src/lib/loshonKodesh.ts:475`.

מסקנה: הכלל הלשוני נכון, אבל יש שני מימושים. ההמלצה היא פונקציית מנצפ״ך אחת, שמופעלת כשער סופי; בדיקות מוקדמות יכולות לקרוא לאותה פונקציה במצב audit בלבד.

## 11. שרת Python המקומי

קובץ: `server/transcribe_server.py`.

### endpoints עיקריים

| endpoint | שורה | תפקיד |
|---|---:|---|
| `/transcribe` | 1370 | תמלול תגובה אחת |
| `/transcribe-stream` | 1554 | תמלול SSE לייצור |
| `/transcribe-live` | 2055 | צ'אנקים בזמן אמת |
| `/align-text` | 4196 | יישור טקסט ואודיו |
| `/align-text-stream` | 4320 | יישור streaming |
| `/enhance-audio` | 3666 | שיפור אודיו |
| `/lk/transcribe` | 5719 | מסלול LK ישן עם post-processing ב־SQLite |
| `/lk/benchmark/run` | 6325 | Benchmark ישן של LK |

### מה קורה במסלולים הרגילים

`_resolve_prompt_and_hotwords` בשורה 251 בונה prompt ו־hotwords. במסלולים הרגילים זה משפיע על הפענוח של Whisper, אך אינו מפעיל את טבלאות SQLite לתיקון הטקסט.

### מה קורה במסלול הישן `/lk/transcribe`

1. מכריח עברית ולשון הקודש.
2. מפעיל prompt ו־hotwords.
3. מתמלל.
4. מפעיל `_lk_apply_postprocessing` בשורה 5547.
5. קורא את `lk_grammar_rules` ואת `lk_dictionary`.
6. שומר `lk_sessions`.

העמוד הישן `LashoKodesh.tsx:144` היה קורא למסלול הזה ואז מפעיל שוב `applyLoshonKodeshReplacements` בדפדפן. זו הייתה כפילות אמיתית במסלול הישן, אך המסלול אינו נגיש דרך הראוטר הנוכחי.

## 12. מצב SQLite שנבדק ישירות

| טבלה | מספר שורות |
|---|---:|
| `lk_dictionary` | 0 |
| `lk_grammar_rules` | 14 |
| `lk_sessions` | 0 |

מתוך 14 הכללים, שישה פעילים:

| spoken | correct | קיים גם בדפדפן |
|---|---|---:|
| תוירה | תורה | כן |
| קוידש | קודש | כן |
| מוישה | משה | כן |
| שאבעס | שבת | כן |
| שאבס | שבת | כן |
| אמעס | אמת | כן |

שמונה כללים ישנים מושבתים. הם כוללים כיוונים מסוכנים כגון תקין אל הגייה, וכללי no-op. הם אינם משנים טקסט כיום, אך אין להחזיר אותם לפעילות.

מסקנה: SQLite אינו מכיל כיום ידע ייחודי פעיל. אפשר לייצא גיבוי, לכתוב בדיקת migration שמוכיחה שששת הזוגות קיימים במקור המרכזי, ואז להסיר את שכבת post-processing של `/lk/transcribe`.

## 13. Supabase ו־Edge Functions

### טבלאות שקשורות לזרימה

| טבלה | בעלות |
|---|---|
| `transcripts` | תמלול ראשי, אודיו, טקסט ערוך |
| `transcript_versions` | גרסאות תמלול חוזר והשוואה |
| `transcription_jobs` | תורים, התקדמות ותוצאות |
| `torah_lexicon_terms` | מילון מונחים מרכזי |
| `asr_learned_corrections` | תיקונים נלמדים מסונכרנים |
| `asr_pipeline_events` | Trace ויומן פייפליין |
| `asr_training_runs` | ריצות אימון ישנות |
| `asr_pending_corrections` | הצעות שממתינות לאישור |
| `comparison_runs` | תוצאות השוואה |
| `lora_training_jobs` | משימות אימון LoRA |

### Edge Functions של תמלול

- `transcribe-openai`
- `transcribe-groq`
- `transcribe-google`
- `transcribe-assemblyai`
- `transcribe-deepgram`
- `transcribe-gemini`
- `transcribe-lovable-stt`
- `process-transcription`
- `loshon-kodesh-ai`
- `gemini-health`

ה־Edge Functions מפיקות טקסט גולמי או תיקון AI. צינור התיקונים הדטרמיניסטי נמצא בצד TypeScript של האפליקציה. זו החלטה סבירה כל עוד כל מסלול ייצור עובר אחר כך ב־`applyTranscriptionKnowledgeWithAi`.

## 14. מסלולים שעוקפים את צינור הידע

| מסלול | קובץ ונקודה | למה עוקף | החלטה |
|---|---|---|---|
| Benchmark שיפור אודיו | `src/pages/Benchmark.tsx:314` | צריך למדוד פלט גולמי | להשאיר, לתייג `raw-evaluation` |
| מנוע שיפור אודיו | `src/lib/audioEnhancement.ts:104` | תמלול לצורך מדידת איכות האודיו | להשאיר גולמי |
| בדיקת פרופיל | `src/components/PronunciationProfileSelector.tsx:223` | דוגמת אבחון | להשאיר אך לתעד raw |
| יבוא אודיו ללמידה | `src/components/training/ReferenceAudioImporter.tsx:53` | יצירת evidence | לשמור raw + provenance |
| פקודות קוליות | `src/pages/VoiceCommandAdmin.tsx:358` | זיהוי פקודה, לא מסמך | להפריד |
| שדה קלט קולי | `src/components/VoiceInputFAB.tsx:101` | dictation קצר | להפריד או להוסיף מצב opt-in |
| YouTube פתיחה ישירה בעורך | `src/pages/YouTube.tsx:281` | טוען output קיים ישירות | דורש החלטה מפורשת: raw או processed |

החריג החשוב הוא YouTube: `openInEditor` טוען טקסט קיים ישירות לעורך בלי צינור הידע ובלי Trace. אם המשתמש מצפה שזה יהיה תמלול ייצור מתוקן, צריך להעביר גם אותו דרך הצינור; אם הוא מיועד לראיית הפלט המקורי, יש לתייגו בבירור כ־Raw ולשמור גרסה מעובדת נפרדת.

## 15. כפילויות וסיווגן

| ממצא | סוג | ראיה | חומרה | פעולה |
|---|---|---|---|---|
| ששת כללי SQLite קיימים גם בדפדפן | כפילות נתונים מוכחת | השוואה מכנית של כל הזוגות | בינונית | גיבוי והסרת SQLite postprocess |
| `LashoKodesh.tsx` מפעיל שרת ואז תיקוני דפדפן | כפילות זרימה רדומה | קוד בשורה 144; אין Route פעיל | בינונית | להסיר לאחר העברת יכולות חסרות |
| `AsrTraining.tsx` מול המעבדה | כפילות UI רדומה | Route מפנה למעבדה | בינונית | להעביר sync חסר ואז להסיר |
| correction כללי + profile correction | כפילות סמכות פעילה | `personalPronunciationModel.ts:144-167` | גבוהה | ישות אחת עם scope/profileId |
| מנצפ״ך בשני מנועים | כפילות מימוש פעילה | `loshonKodesh.ts` וגם `hebrewRuleEngine.ts` | בינונית | פונקציה אחת ושער סופי אחד |
| prompt/hotwords ב־TS וב־Python | כפילות קונפיגורציה | `transcriptionHotwords.ts`, `loshonKodesh.ts`, `_resolve_prompt_and_hotwords` | גבוהה | להעביר payload resolved אחד לשרת |
| `/transcribe` במעבדה מול `/transcribe-stream` בייצור | סטיית מסלול | קוד פעיל | גבוהה למדידה | מצב production-stream במעבדה |
| כלי עזר ישירים ל־`/transcribe` | הפרדה מכוונת | מטרות שונות | נמוכה | לתייג raw/utility ולא לאחד בכוח |

## 16. הבדיקות ההפוכות והאדוורסריות

הבדיקות החדשות ב־`src/lib/transcriptionTrace.test.ts` מוכיחות:

1. כל שינוי דטרמיניסטי ניתן לשחזור מהקלט ורשימת הפעולות.
2. לכל שלב יש קובץ מקור ופונקציית מקור.
3. תיקון אנושי נלמד מוגן, כלל LK סותר אינו פועל עליו, והטקסט משוחזר.
4. גבול AI מתועד כפעולה אטומה עם קלט ופלט מלאים.
5. שער מנצפ״ך לאחר AI עדיין מתקן פלט צורני שגוי.
6. שינוי טקסט בלי פעולה מתועדת מסומן כלא תקין.
7. occurrence שמצביע למיקום או טקסט מקור שגוי נכשל בשחזור.
8. שרשרת עם checksum לא מחובר או index מדולג נפסלת.
9. הפעלה חוזרת של הצינור על תוצאה תקינה היא idempotent: אין שינוי נוסף ואין פעולות נוספות.

בדיקות מנצפ״ך ב־`src/lib/loshonKodesh.test.ts` מכסות את כל חמשת הזוגות, סוף מילה, אמצע מילה, תחילת מילה, ניקוד, פיסוק, מקף וראשי תיבות.

## 17. מה בוצע בקוד במסגרת המיפוי

1. נוסף מודל Trace ב־`src/lib/transcriptionTrace.ts`.
2. נוספו occurrences מדויקים ל־`src/lib/hebrewTextReplacement.ts`.
3. תיקונים נלמדים, מילון, פרופיל, LK וכללים מוחלטים מחזירים פעולות Trace.
4. צינור הידע נבנה כשרשרת של 9 שלבים דטרמיניסטיים ועוד AI ושער post-AI.
5. שמירת Trace אוחדה ב־`src/lib/pipelineAudit.ts`.
6. החיבור הופעל במעבדה, במסך התמלול הראשי ובתמלול חוזר.
7. נוספה הגנה שמונעת מכשל/quota ב־localStorage להפיל שמירת תמלול.
8. נוספו בדיקות שחזור, סתירה, idempotence, שרשרת שבורה ואחסון חסום.
9. בדיקות ישנות עודכנו כדי לאמת גם offsets ולא רק טקסט וספירה.

## 18. מה עדיין לא הוכח

1. לא בוצע בתיעוד הזה ניסוי שמע אמיתי מול כל ספקי הענן, משום שניסוי כזה צורך מפתחות, מכסות ועלות. בדיקות הדפדפן משתמשות ב־responses מבוקרים.
2. לא נבדקה שקילות מלאה בין `/transcribe` לבין `/transcribe-stream` על corpus אמיתי.
3. לא בוצעה ספירת נתונים חיה בכל טבלאות Supabase; קיימות המיגרציות והקריאות בקוד מופו.
4. Trace יכול להראות קלט ופלט AI, לא reasoning פנימי של Gemini או ספק אחר.
5. Trace runtime מזהה חפיפות שהתממשו באותה ריצה; הוא אינו מחליף סורק סטטי של כל הכללים האפשריים.
6. תזמוני מילים נשארים תזמוני המנוע הגולמי גם כאשר תיקון טקסט מוסיף/מוחק מילים. נדרש בעתיד alignment מחדש או מפת raw-token אל corrected-token.

## 19. ארכיטקטורת היעד המומלצת

### 19.1 מעטפת תוצאה אחת

כל מנוע צריך להחזיר `TranscriptionResultEnvelope` אחד:

```ts
type TranscriptionResultEnvelope = {
  recordingId: string;
  recordingFingerprint: string;
  rawText: string;
  rawWordTimings: WordTiming[];
  correctedText: string;
  correctedAlignment?: CorrectedTokenAlignment[];
  engine: EngineProvenance;
  knowledgeConfigSnapshot: KnowledgeConfigSnapshot;
  trace: TranscriptionTraceStage[];
  traceValidationErrors: string[];
};
```

כך אין ערבוב בין טקסט גולמי לטקסט מתוקן, והתזמונים אינם מוצגים כאילו הם מתאימים אוטומטית לטקסט ששונה.

### 19.2 מקור אמת אחד לכל סוג מידע

| סוג מידע | מקור אמת מומלץ |
|---|---|
| אודיו | `transcripts` + Storage, fingerprint יציב |
| טקסט גולמי | גרסת `raw` בלתי ניתנת לדריסה |
| טקסט אמת | גרסת `gold` עם Human Review ו־provenance |
| מונחים | `torah_lexicon_terms` |
| תיקוני ASR | `asr_learned_corrections` עם scope/profileId |
| כללי ברזל | קוד versioned + tests |
| Trace | `asr_pipeline_events` או טבלת trace ייעודית לפי run |
| אימון | snapshots שמפנים ל־recordingId ול־goldVersionId |

### 19.3 עקרון סדר

- acoustic decoding: מנועי Whisper/Gemini/אחרים.
- decoder hints: prompt/hotwords, ללא שינוי כפוי של הטקסט.
- deterministic normalization: כללי ברזל בלבד.
- contextual knowledge: מילון ותיקונים מאומתים לפי scope.
- optional AI adjudication: רק במצב מפורש, עם שמירת raw.
- final orthography gate: מנצפ״ך אחד, תמיד אחרון.
- persistence: raw, corrected, Gold ו־Trace כישויות נפרדות ומקושרות.

## 20. סדר העבודה המומלץ מכאן

### P0: ראיות ושלמות נתונים

1. לשמור את ה־Trace הקיים ולחייב `traceValidationErrors.length === 0` לפני אישור Gold.
2. להוסיף correlation בין Trace לבין `transcriptId`, ‏`transcriptionJobId` ו־fingerprint גם במסלול הייצור.
3. לא לשמור תזמוני raw כאילו הם תזמוני corrected; לסמן alignment status.
4. להוסיף בדיקה ל־YouTube שמכריעה raw מול processed ומונעת מצב לא מסומן.

### P1: איחוד סמכויות

1. לאחד correction כללי ופרופיל לרשומה אחת עם scope.
2. לאחד מימושי מנצפ״ך לפונקציה אחת.
3. להפוך `buildTranscriptionHotwords` ליצרן היחיד של hotwords.
4. לשלוח ל־Python snapshot מוכן של prompt/hotwords, במקום defaults מקבילים בשני הצדדים.

### P2: סילוק קוד רדום

1. לייצא גיבוי JSON של SQLite ושל localStorage הישן.
2. להוסיף בדיקת migration שמוכיחה שכל ידע פעיל הועבר.
3. להסיר את `/lk/transcribe` postprocess ואת מסך `LashoKodesh.tsx`.
4. להעביר יכולת sync שעוד נדרשת מ־`AsrTraining.tsx` לשירות מרכזי.
5. להסיר את `AsrTraining.tsx` לאחר בדיקת route/import חוזרת.

### P3: שקילות מעבדה וייצור

1. להוסיף במעבדה מנוע `Local CUDA production-stream`.
2. להריץ אותו corpus דרך `/transcribe` ו־`/transcribe-stream`.
3. להשוות raw text, timings, model config ו־Trace.
4. רק לאחר שקילות, להשתמש במעבדה כשער regression מחייב.

### P4: אימון מודל

רק אחרי P0-P3:

1. להקפיא benchmark שאינו נכנס לאימון.
2. להשתמש ב־Gold אנושי, לא בפלט Gemini לבדו.
3. Gemini יכול לייצר Silver ולהצביע על מועמדים לבדיקה.
4. לאמן LoRA/adapter רק על audio + verified transcript + provenance.
5. לקדם מודל רק אם הוא משפר WER, Term Recall ו־regression slices בלי לפגוע בעברית כללית.

## 21. תנאי השלמה לפרויקט מסודר

המערכת תיחשב מסודרת כאשר:

- כל מסלול ייצור מחזיר `TranscriptionResultEnvelope`.
- כל שינוי טקסט מופיע ב־Trace או גורם לכשל איכות.
- לכל כלל יש מזהה, owner, קובץ, גרסה וסיבה.
- אין אותו correction בשני מאגרים עצמאיים.
- אין defaults נפרדים ל־hotwords ב־TS וב־Python.
- אין מסכים ישנים שאינם נגישים אך ממשיכים להכיל לוגיקה עסקית.
- raw, corrected ו־Gold לעולם אינם נדרסים זה על זה.
- כל אימון מצביע לאודיו המקורי ולטקסט אמת מאושר.
- המעבדה יכולה להריץ מחדש אותה הקלטה, באותה תצורה, ולהראות שיפור או רגרסיה במספרים.

## 22. שורה תחתונה

אין צורך לזרוק את המערכת. צריך לבצע **קונסולידציה מבוקרת**:

1. לשמור את צינור הידע המרכזי וה־Trace החדש.
2. להשאיר את המעבדה נפרדת כתפקיד מדידה, אך להשתמש באותו שירות ייצור.
3. לבטל כפילויות של סמכות ומקורות נתונים.
4. להסיר קוד רדום רק אחרי export ובדיקת migration.
5. לאמן מנוע קולי רק על Audio + Gold אנושי; להשתמש ב־Gemini כ־Silver/מורה מסייע, לא כאמת אוטומטית.

זה נותן מערכת אחת מבחינת אמת, עקיבות וידע, אך שומר הפרדה מקצועית בין תמלול, מדידה, למידה וכלי עזר.

## 23. ההמלצה המקצועית שלי

### 23.1 ההכרעה המרכזית

לא להתחיל את הפרויקט מחדש, ולא להוסיף עוד מנגנון תיקונים. הבסיס הנכון כבר קיים: מנועי תמלול שמחזירים Raw, צינור ידע מרכזי, מעבדת A/B, Gold אנושי ו־Trace. הפעולה הנכונה היא **קונסולידציה מבוקרת סביב מקור אמת אחד**, תוך שמירת ההפרדה בין תמלול ייצור, מדידה, למידה וכלי עזר.

היעד צריך להיות פשוט להסבר:

1. כל מנוע מפיק תמלול גולמי שאינו משתנה בדיעבד.
2. שירות עיבוד עברית אחד מפיק גרסה מתוקנת ומתעד כל שינוי.
3. אדם מאשר או מתקן ומייצר Gold נפרד.
4. רק Gold מאושר נכנס לנתוני אימון.
5. כל ניסוי חוזר לאותו Audio ID ומשווה את אותה הקלטה באותן הגדרות.

### 23.2 מה לבצע תחילה

#### צעד ראשון: לנעול ראיות לפני ניקוי

- לחבר לכל Trace את מזהה ההקלטה, התמלול, גרסת המנוע, ההגדרות ו־fingerprint של האודיו.
- לשמור Raw, Corrected ו־Gold כרשומות נפרדות ומקושרות. אסור ששמירה חדשה תדרוס אחת מהן.
- למנוע אישור Gold אם שרשרת ה־Trace שבורה או אם האודיו אינו נגיש.
- לסמן במפורש אם תזמוני המילים שייכים ל־Raw או עברו alignment לגרסה המתוקנת.

**תנאי הצלחה:** ניתן לפתוח כל מילה מתוקנת ולראות מה היה הטקסט לפני השלב, מה אחריו, איזה כלל פעל, מאיזה קובץ ופונקציה הוא הגיע, ובאיזה run זה קרה.

#### צעד שני: לאחד מקורות סמכות כפולים

- לאחד תיקון כללי ותיקון פרופיל לישות Correction אחת עם `scope` ברור: גלובלי או פרופיל מסוים.
- לאחד את שני מימושי מנצפ״ך לפונקציה דטרמיניסטית אחת. מותר להפעילה בתחילת הצינור לצורכי ניקוי, אך חובה להפעיל שער סופי לאחר כל AI.
- לקבוע יצרן יחיד ל־hotwords ול־prompt. השרת יקבל snapshot מוכן מהלקוח או משירות תצורה מרכזי, ולא יחזיק ברירות מחדל מתחרות.
- לקבוע שהמילון מציע ומטה את המנוע, אך אינו מבצע החלפה עיוורת כאשר קיימות כמה מילים חוקיות בהקשרים שונים.

**תנאי הצלחה:** אותו תיקון אינו נשמר בשני מאגרים עצמאיים, וכל הפעלה של כלל מצביעה על רשומת מקור יחידה.

#### צעד שלישי: להסיר קוד רדום רק לאחר הוכחת הגירה

- לייצא את כללי SQLite ואת הגדרות ה־localStorage הישנות לקובץ גיבוי קריא.
- להשוות אוטומטית שכל כלל פעיל קיים במקור האמת החדש ושאותה קבוצת בדיקות עוברת בשני הנתיבים.
- לאחר הוכחת שקילות, להסיר את postprocess של `/lk/transcribe`, את מסך `LashoKodesh.tsx` הישן ואת מסך `AsrTraining.tsx` הישן.
- להשאיר redirect זמני עם לוג שימוש, כדי לגלות קישור חיצוני או סימנייה ישנה לפני מחיקה סופית.

**תנאי הצלחה:** חיפוש import ו־route אינו מוצא לוגיקה עסקית במסכים הרדומים, ולא אבד אף כלל פעיל או נתון משתמש.

#### צעד רביעי: להשוות את המעבדה לייצור

- להוסיף למעבדה מצב שמפעיל בדיוק את endpoint הייצור, כולל streaming והגדרות זהות.
- להריץ corpus קבוע דרך מסלול המעבדה ומסלול הייצור ולהשוות Raw, timings, תצורת מנוע ו־Trace.
- לשמור benchmark קפוא שאינו משמש לאימון ולחלק אותו לפרוסות: עברית כללית, לשון הקודש, ארמית, שמות, ציטוטים ורעש.
- לקבוע שער קידום: שיפור ב־WER וב־Term Recall ללא רגרסיה מעבר לסף מוסכם באף פרוסה חשובה.

**תנאי הצלחה:** אותה הקלטה ואותה תצורה מפיקות אותה תוצאה גולמית בשני המסלולים, או שהבדל מתועד ומוסבר.

#### צעד חמישי: להתחיל אימון קולי רק על נתונים אמינים

- להשתמש ב־Gemini ובמנועים חזקים כמקור Silver וכמאתרי מועמדים לתיקון, לא כ־Gold אוטומטי.
- תיקון נקודתי של אדם יכול להפוך ל־Gold רק כאשר הוא קשור לקטע האודיו הנכון, עם גבולות זמן ו־provenance.
- להבחין בין תיקון ASR לבין עריכה סגנונית: החלפת מילה ששמעה לא נכון יכולה ללמד את המודל; פיסוק, ניסוח מחדש או סיכום אינם תווית קולית.
- לאמן LoRA או adapter רק לאחר שקיים benchmark קפוא ומנגנון rollback לגרסת המודל הקודמת.

**תנאי הצלחה:** מודל חדש אינו מקודם לפי התרשמות. הוא מקודם רק כאשר EvaluationRun שמור מוכיח שיפור ומאפשר שחזור מלא.

### 23.3 מה להשאיר נפרד בכוונה

| רכיב | למה לא לאחד אותו לתוך הייצור |
|---|---|
| Raw transcription | הוא ראיית המקור להשוואה ולחקירת תקלות |
| Corrected transcription | הוא תוצר צינור הכללים וה־AI, לא אמת אנושית |
| Gold | הוא אמת מאושרת שמותר להשתמש בה לאימון והערכה |
| Benchmark | חייב להישאר מחוץ לאימון כדי למדוד הכללה אמיתית |
| Audio enhancement | צריך להיות variant מתועד של אותו אודיו, לא החלפה של המקור |
| כלי פקודות קוליות ושדות דיבור | מטרתם זיהוי פקודה קצרה, לא תמלול שיעור מלא |

### 23.4 מה לא לעשות

- לא ליצור כלל גלובלי מסוג `ילדים -> ילדות` על בסיס תיקון יחיד. זה תיקון תלוי אודיו והקשר, לא חוק עברי מוחלט.
- לא לתת ל־Gemini לכתוב ישירות Gold בלי אישור אנושי.
- לא למחוק את האודיו המקורי לאחר ניקוי רעשים או המרה.
- לא לערבב טקסט benchmark עם נתוני אימון.
- לא להסיר SQLite, מסכים ישנים או localStorage לפני export והוכחת שקילות.
- לא להסתמך על WER בלבד; במערכת תורנית חובה למדוד גם Term Recall ושגיאות בשמות ובציטוטים.
- לא להוסיף מנגנון חדש לפני שבודקים אם אותה יכולת כבר קיימת בצינור, במילון, בפרופיל או בשרת.

### 23.5 סדר ביצוע מעשי מומלץ

1. שבוע ראשון: correlation מלא ל־Trace, הפרדת Raw/Corrected/Gold ובדיקות שלמות נתונים.
2. שבוע שני: איחוד Correction ומנצפ״ך למקורות אמת יחידים.
3. שבוע שלישי: איחוד prompt/hotwords והוספת מסלול production-stream למעבדה.
4. שבוע רביעי: export, בדיקת migration והסרת הנתיבים הרדומים.
5. לאחר מכן: בניית benchmark קפוא, איסוף Gold והפעלת ניסוי אימון ראשון.

הסדר חשוב: אם מאמנים לפני איחוד הסמכויות והוכחת ה־Trace, עלולים ללמד מודל מנתונים שנדרסו או שאינם קשורים במדויק לאודיו. אם קודם נועלים provenance ומדידה, כל שיפור עתידי יהיה ניתן להוכחה, לשחזור ולביטול.

## 24. תגובה מאומתת לממצאי ריצת LoRA

הסעיף הזה מגיב לטענות שהועברו לאחר השלמת ריצת `psalm_chant_ashkenazi_v1_fixed`. כל טענה מסווגת כאן לפי מצב הראיות הנוכחי, ולא לפי מספר הסוכנים שתמכו בה.

### 24.1 התקלה בכלי האימות של המפתח

התיקון העקרוני נכון: אם כל המפריכים קרסו, אסור להסיק שהטענה שרדה. מצב כזה צריך להיות `unverified` או `insufficient-evidence`, ולא ערך בוליאני חיובי. תנאי קבלה תקין צריך לדרוש מספר מינימלי של מאמתים שסיימו בפועל, להפריד בין `refuted = false` לבין `verified = true`, ולשמור את מספר המאמתים שהצליחו והסיבות לכשל האחרים.

קובץ ה־workflow שהוזכר אינו חלק מעץ הפרויקט שנבדק, ולכן ניתן לאשר את נכונות העיקרון אך לא את המימוש המדויק של 29 השורות שנוספו בו.

### 24.2 טבלה מסכמת של הטענות

| טענה | הכרעה | ראיה | המשמעות |
|---|---|---|---|
| ריצת האימון הושלמה | נכון, עם הסתייגות | `progress.json` מסמן `done`, נשמר adapter וקיימת תיקיית CT2; בלוג מופיע כשל merge/convert ישן | יש artifact, אך היסטוריית ההמרה אינה נקייה ויש לבדוק טעינה בפועל לפני שימוש |
| ה־evaluation היה פיצול אקראי מאותה הקלטה | **לא נכון לפי הראיות הקיימות** | לוג הריצה אומר `Using recording-group holdout with 23 evaluation pairs`; ה־manifest הנוכחי מפריד קבוצה אחת של 76 לאימון וקבוצה אחרת של 23 להערכה | אין בסיס לקרוא למדד "שינון מאותה הקלטה" |
| היעדר `eval_fingerprint` מבטל את המדד | נכון חלקית | ב־progress הישן אין fingerprint ולכן אי אפשר להוכיח בדיעבד את הזהות המדויקת של קובצי ה־eval; אך היעדרו אינו מוכיח שנעשה random split | המדד הישן אינו בר־שחזור מלא, אך קיימת ראיה חיובית ל־recording holdout |
| המודל הפעיל אינו נטען | נכון | `resolve_trained_model` מחזיר `None`, וכך גם `get_active_ct2_path` | התמלול אינו משתמש כיום במודל LoRA הזה |
| הכשל נובע רק מחוסר CER ברישום | נכון לרישום, אך לא כל הסיפור | `trained_models.json` מכיל WER בלבד ולכן השער מחזיר `missing WER/CER holdout metrics` | הרישום הישן אינו תואם לשער החדש |
| המודל היה עובר אילו היינו מעתיקים את המדדים מ־progress | **לא נכון** | WER השתפר מ־80.30% ל־73.98%, אבל CER החמיר מ־39.49% ל־41.38% | גם עם fingerprint ורישום מלא, שער האיכות הנוכחי צריך לדחות את המודל |
| ההשבתה שקטה לגמרי | נכון חלקית | פונקציות resolution בולעות exception ומחזירות `None`; מנגד endpoint של active model מחזיר `suspended` וסיבה, ובחירה מפורשת במודל לא מוכר מוחזרת כשגיאת HTTP | צריך לוג שרת ברור יותר, אך אין fallback סמוי שמוכח כטוען מודל בסיס תחת אותו מזהה |
| `quality_counts` ו־`label_source_counts` אינם משתתפים בשער | נכון | הם מחושבים ב־`training_routes.py:197-203`, מוצגים בממשק, אך `_model_quality_gate` אינו קורא אותם | אפשר לאמן דאטהסט ללא הוכחת Gold |
| 99 הקטעים הם שתי נקודות נתונים | ניסוח מוגזם | יש 99 קובצי קטעים שונים ו־99 hashes שונים, אך רק שתי הקלטות מקור ושתי קבוצות | זה אינו דאטהסט של שתי שורות, אך מגוון הדוברים/ההקלטות קטן מדי להכללה מקצועית |
| בסיס המודל הוא `ivrit-ai/whisper-large-v3` | נכון | מופיע בלוג הריצה וב־adapter config | הניסוי הוא התאמה של בסיס עברי חזק, לא התחלה ממודל כללי חלש |
| קובץ המונחים מכיל 62 שמות | לא מדויק | יש 62 שורות פיזיות, אך רק 53 מונחים אפקטיביים לאחר הסרת הערות ושורות ריקות; הרשימה כוללת גם מושגי הלכה ולא רק שמות | יש לדווח 53 מונחים אפקטיביים ולגרס את הרשימה |
| Term Recall נדלג על נתוני תהילים | נכון | אף אחד מ־53 המונחים האפקטיביים אינו מופיע ב־reference של קבוצת ההערכה; `_term_recall` מחזיר `None` כאשר המכנה אפס | היעדר מדד אינו הצלחה ואינו כשל; זו פרוסת eval שאינה בודקת את מטרת המונחים |

### 24.3 מצב הדאטהסט בפועל

הקובץ `server/lora_runs/datasets/psalm-chant-ashkenazi/dataset_meta.json` מציג:

- 99 קטעים בסך הכול.
- 76 קטעים לאימון מהקלטת מקור אחת.
- 23 קטעים להערכה מהקלטת מקור אחרת.
- 99 נתיבי אודיו ו־99 hashes ייחודיים.
- שתי קבוצות הקלטה בלבד.
- 1,065.39 שניות אודיו, כ־17.8 דקות.
- כל 99 הקטעים מסווגים `unknown` מבחינת quality tier.
- כל 99 הקטעים מסווגים `unknown` מבחינת label source.
- למרות זאת `ready_for_training` הוא `true` ואין warnings.

המסקנה: ההפרדה בין הקלטות טובה יותר מן הנטען בביקורת, אך איכות ה־provenance חלשה יותר מן הרצוי. שני קובצי מקור אינם מספיקים כדי למדוד הכללה לדוברים, ניגונים, חדרים, מיקרופונים וסוגי שיעור שונים.

### 24.4 פער metadata נוסף שהתברר

רוב מסלולי האישור החדשים משתמשים ב־`buildApprovedAsrMetadata` וכותבים `qualityTier`, ‏`labelSource` ו־`sourceRecordingId`. לעומת זאת, `AudioLearningQueue.tsx` עדיין כותב שדות ישנים כגון `source` ו־`reviewStatus`, בלי להשתמש בבונה המטא־דאטה הקנוני. השרת סופר רק `qualityTier/quality_tier` ו־`labelSource/label_source`, ולכן קטעים מהמסלול הישן עלולים להופיע כ־`unknown` אף שאדם אישר אותם.

זהו ממצא של חוסר אחידות בסכמת metadata, ולא הוכחה שהתמלול עצמו שגוי. יש לאחד את כל כותבי הדאטה דרך `buildApprovedAsrMetadata` ולהוסיף migration למטא־דאטה קיים.

### 24.5 מה נכון לעשות בעקבות הממצאים

1. לא להפעיל את `psalm_chant_ashkenazi_v1_fixed` כמודל ייצור. הוא נכשל ב־CER ובר־השחזור שלו חסר fingerprint היסטורי.
2. לא למחוק את ה־adapter. לשמור אותו כ־artifact ניסויי עם סטטוס `rejected`, כדי ללמוד ממנו ולבדוק את תהליך הטעינה.
3. להפוך איכות תיוג לשער קשיח: אימון אמיתי ידרוש אחוז מינימלי של Gold אנושי, אפס `unknown`, ו־label source מזוהה לכל קטע.
4. לתקן את מסלול `AudioLearningQueue` כך שישתמש באותה סכמת metadata של המעבדה ושל יבוא אודיו.
5. לדרוש לפחות כמה הקלטות מקור נפרדות באימון וכמה הקלטות נפרדות ב־holdout. שתי הקלטות הן בדיקת smoke מחוזקת, לא benchmark מקצועי.
6. להגדיר שתי פרוסות הערכה לפחות: תהילים/קריאה וקטעי שיעור עם שמות ומונחים. Term Recall יחושב רק בפרוסה שבה קיימים מונחי יעד, והיעדר חפיפה ידווח `not-applicable` במקום להיעלם.
7. לשמור בכל ModelRegistry את כל המדדים, fingerprint, ספירת דוגמאות, קבוצות הקלטה, איכות תיוג, גרסת term list וגרסת קוד האימון.
8. להוסיף לוג מפורש כאשר מודל מבוקש נפסל בשער, כולל סיבת הדחייה, בלי לבלוע exception ללא trace תפעולי.

### 24.6 אימות שבוצע בעקבות הביקורת

- הורצה ישירות `_model_quality_gate` על הרשומה הקיימת: נכשל בגלל מדדי WER/CER חסרים ברישום.
- הורצו `resolve_trained_model` ו־`get_active_ct2_path`: שניהם החזירו שאין מודל פעיל.
- נספרו manifest מלא, train ו־eval ונבדקו group IDs ו־hashes.
- נספרו 53 מונחי יעד אפקטיביים ונמצאה אפס חפיפה עם reference ההערכה.
- הורצו 14 בדיקות Python של dataset split ושער האיכות; כולן עברו, כולל מניעת זליגה בין recording groups ודחיית CER regression.

המסקנה המקצועית: הביקורת זיהתה נכון שהמודל אינו פעיל, ששער איכות התוויות חסר ושמדד המונחים אינו רלוונטי ל־eval של תהילים. היא טעתה בהסקה שה־eval היה פיצול אקראי מאותה הקלטה, והגזימה כאשר כינתה 99 קטעים "שתי נקודות". המצב אינו אובדן דרך, אך לפני אימון נוסף חייבים לנעול provenance, איכות תוויות וגיוון הקלטות.

## 25. מצב המערכת לאחר ביצוע האיחודים

הפרק הזה מבדיל בין כפילות שהוסרה, הפרדה שנשמרה בכוונה וקוד legacy שבודד. פרסום ה־frontend ב־Lovable הושלם ונבדק לאחר כתיבת המיפוי הראשוני.

### 25.1 מפת סמכות עדכנית

| תחום | מקור הסמכות לאחר האיחוד | צרכנים פעילים | מצב |
|---|---|---|---|
| תיקונים אנושיים נלמדים | `src/lib/correctionRepository.ts` | עורך, hook למידה, פרופיל הגייה, סנכרון ענן | מאוחד מקומית ובענן; בדיקת constraints עברה |
| אורתוגרפיית מנצפ״ך | `src/lib/hebrewOrthography.ts` | צינור הידע, מנוע הכללים, wrapper לשון הקודש | מאוחד ומוגן בשער סופי |
| Prompt ומונחי LK סטטיים | `shared/hebrew_transcription_defaults.json` | `src/lib/loshonKodesh.ts`, `server/hebrew_transcription_defaults.py` | מאוחד בין לקוח לשרת |
| תמלול CUDA מקומי | `/transcribe-stream` | מסך ייצור ומעבדת A/B | מאוחד ברמת endpoint וחוזה SSE |
| שינוי טקסט ו־Trace | `applyTranscriptionKnowledgeWithAi` ו־`logKnowledgeTrace` | ייצור, תמלול חוזר ומעבדה | אותו מנוע; המעבדה שומרת audit מלא |
| metadata לאימון | `buildApprovedAsrMetadata` ושערי `training_routes.py` | Gold, יבוא, תור למידה, אימון LoRA | מאוחד; legacy unknown נחסם |
| LK SQLite ישן | export גרסאי + `ENABLE_LEGACY_LK_API=1` בלבד | אין צרכן פעיל כברירת מחדל | quarantine, HTTP 410 |

### 25.2 מה אינו כפילות ולכן נשאר נפרד

- Raw נשמר בנפרד מ־Corrected כדי לראות מה המנוע שמע ומה הכללים שינו.
- Gold נשמר בנפרד משניהם מפני שרק הוא אמת אנושית לאימון.
- Benchmark קפוא נשמר מחוץ לאימון כדי למנוע leakage.
- original audio נשמר גם כשנוצר processed variant; שניהם חולקים recording fingerprint אך מתועדים כקלטים שונים.
- מנוע A ומנוע B נשארים נפרדים כתוצאות ניסוי, אך שניהם עוברים באותו חוזה Pipeline ו־Trace.
- בדיקות זמינות, preload, פקודות קוליות ודיבור קצר אינן מאוחדות לתמלול שיעור, משום שמטרתן וחוזה התוצאה שונים.

### 25.3 שרשרת תמלול קנונית

1. קובץ המקור מזוהה ומקבל fingerprint.
2. הגדרות המנוע, המודל, סוג החומר, מונחי היעד ועיבוד האודיו נחתמות ב־configuration event.
3. local engine נשלח ל־`/transcribe-stream`; cloud engine עובר ב־runner הייעודי אך מחזיר אותו מבנה תוצאה.
4. ה־Raw והתזמונים נשמרים לפני כל תיקון.
5. שער מנצפ״ך ראשוני מתקן רק צורה עברית מחייבת.
6. תיקונים מאומתים נטענים מה־scope הנבחר בלבד.
7. מסכת ההגנה מונעת מכללי LK לשבור תיקון שנלמד.
8. מילון, כללים דטרמיניסטיים ו־LK פועלים בסדר מתועד.
9. המסכה משוחזרת ונרשם מי שינה כל טווח ומאיזה קובץ ופונקציה.
10. אם AI מופעל, נשמר boundary נפרד של הקלט והפלט שלו.
11. שער מנצפ״ך סופי מופעל אחרי AI ואינו ניתן לכיבוי.
12. Trace נבדק לשלמות, רציפות וחפיפות; מדדים מחושבים מול Gold בלבד.

### 25.4 ראיות חיוביות והפוכות

| שאלה | ראיה חיובית | ראיה הפוכה/שלילית | מסקנה |
|---|---|---|---|
| האם מנצפ״ך נשאר תקין בסוף? | כל חמש האותיות נבדקו עם ניקוד ופיסוק | AI החזיר בכוונה צורות שגויות וה־final gate תיקן אותן | מחייב בתוצר האחרון |
| האם תיקון פרופיל דורס גלובלי? | קריאה עם profile מחזירה union מדורג | החלפת scope אחד אינה מוחקת את האחר | הפרדה תקינה ללא שני מאגרים פיזיים |
| האם שני מנועים יוצרים תיקון כפול? | אותו source/target נכתב פעם אחת | engine שונה אינו משנה את מפתח הזהות | אין כפילות סמכות |
| האם LK הישן עדיין משנה טקסט? | `/health` ו־`/transcribe-stream` פעילים | `/lk/*` מחזיר 410 כברירת מחדל | legacy אינו פעיל |
| האם המעבדה באמת זהה לייצור המקומי? | שניהם פונים ל־`/transcribe-stream` | mock JSON לנתיב הישן לא השלים ריצה | חוזה SSE הוא מקור האמת |
| האם תיקון אנושי משפיע בריצה חוזרת? | E2E משנה `ארומך` ל־`ארוממך` אחרי אימות | לפני האימות diff נשאר מסומן | הלמידה עובדת רק לאחר אישור |
| האם אודיו בענן מועלה פעמיים? | טעינת מקור קיים שומרת את identity | assertion מוכיח אפס upload נוסף | אין שכפול קובץ |
| האם דאטה לא מסווג נכנס לאימון? | metadata קנוני עובר שער | 99 קטעי legacy עם unknown מסומנים not-ready | אין קידום שקט לאימון |

### 25.5 תוצאות אימות סופיות

- 290 מתוך 290 בדיקות יחידה בצד הלקוח עברו.
- 30 מתוך 30 בדיקות Python עברו עם ResourceWarning כשגיאה.
- 10 מתוך 10 תרחישי Playwright של המעבדה עברו בדפדפן headless מבודד.
- `npx tsc --noEmit` עבר ללא שגיאות.
- build ייצור עבר לאחר transform של 5,187 modules.
- `git diff --check` עבר; קיימות רק אזהרות EOL צפויות ב־Windows.

### 25.6 אימות Supabase שבוצע לאחר כתיבת הדוח

- המיגרציה `20260902193000_unify_asr_correction_scope.sql` הוחלה בהצלחה בפרויקט `pycryoyipkymaqorgpjy` באמצעות ה־runner המתועד.
- schema probe מאומת דרך PostgREST הצליח לקרוא `scope/profile_id`.
- רשומת global ורשומת profile עם אותו source/target נשמרו במקביל.
- כפילות באותו scope נדחתה על ידי unique constraint.
- profile ללא מזהה נדחה על ידי check constraint.
- שתי הרשומות הזמניות נמחקו לפי IDs מדויקים, וקריאת cleanup החזירה אפס.
- סקריפט השחזור הוא `scripts/verify-correction-scope-cloud.mjs`; הוא אינו שומר סיסמה או token.

### 25.7 פערים שנותרו והמלצה אופרטיבית

1. חבילת האיחוד נדחפה לענף `main` ב־commit `7a88a4f`; build הענן עבר, וכשל E2E יחיד זוהה כ־strict locator כפול בשכבת הנגישות של Toast.
2. אורקל ה־E2E תוקן ב־commit `438f947`. ה־spec הרלוונטי עבר 17 מתוך 17 מקומית, וריצת GitHub Actions `33670430977` עברה במלואה: build ירוק, 65 בדיקות E2E עברו ו־4 בדיקות חיצוניות דולגו לפי התצורה.
3. Publish ב־Lovable בוצע ידנית, וה־bundle הציבורי השתנה. ה־chunks החיים כוללים את מאגר התיקונים הקנוני, Trace, אורתוגרפיית מנצפ״ך ומעבדת התמלול החדשה.
4. smoke headless לאחר Publish עבר כאורח וכמשתמש מחובר. המעבדה נטענה ללא שגיאות, ורכיב האודיו בעורך הגיע למצב מוכן ללא שגיאת media.
5. לא למחוק את export ה־SQLite או את rollback flag עד לאחר תקופת יציבות וריצת production מאומתת.
6. להוסיף בעתיד audit persistence מלא גם למסך הייצור, לא רק ל־console/local trace, אם רוצים היסטוריה מרכזית לכל תמלול רגיל.
7. לבנות Gold מכמה הקלטות ודוברים; אין להפעיל את מודל ה־LoRA שנדחה ואין לאמן מחדש על 99 קטעי unknown.

המסקנה: אין צורך להתחיל את המערכת מחדש. מנגנוני הסמכות המרכזיים אוחדו, הנתיב הישן בודד, ההפרדות הנחוצות למדידה נשמרו, סכמת התיקונים בענן נבדקה התנהגותית, וה־frontend החדש פורסם ונבדק ב־Lovable. התלות היחידה שאינה שירות ענן היא שרת CUDA/Ollama/ניקוד מקומי, ולכן מצב מנותק שלו ב־production אינו מעיד על כשל בפריסה.

### 25.8 בדיקת קבלה מקצה לקצה לאחר Publish

נוסף מסלול QA קנוני שמבדיל בין שלושה סוגי הוכחה:

| שכבה | מה נבדק | מה היא מוכיחה | מה אינה מוכיחה |
|---|---|---|---|
| unit/build | 290 unit, ‏TypeScript ו־build | חוזי פונקציות, טיפוסים ויכולת אריזה | שירותי ענן או UI חי |
| מעבדה מקומית | 10 תרחישי Playwright על build | UI, ‏A/B, ‏Trace, תיקון חוזר, Gold, מקור, audio processing ו־RTL | פריסת Lovable |
| Production Gemini | התחברות, מקור Supabase אמיתי, A/B ושמירת audit | frontend ציבורי, Edge Function, התמדה וניקוי | איכות ללא Gold; הרצה מלאה של 3.5 כאשר יש fallback |
| Frontend מקומי + מקור ענן + CUDA | אותו מקור Supabase מול שרת GPU מקומי | חוזה SSE אמיתי, 77 word timings, זהות מקור ו־Trace | זמינות CUDA למשתמש אחר או בענן |

בכל אחת משתי ריצות production התקבלו 34 אירועים מקומיים ו־34 אירועים בענן, שני comparison runs, כל עשרת שלבי הצינור ו־Trace תקין ללא שגיאת רציפות. מספר העלאות המקור היה אפס, ומוני Gold והמילון לא השתנו. לאחר cleanup נותרו אפס אירועי ניסוי ואפס comparison runs של בדיקות הקבלה.

התברר race ממשי במסלול עורך אל המעבדה: טקסט מתוקן היה עלול לשמש גם כ־initial transcript אם רשימת הענן טרם נטענה. המסלול תוקן כך שרשומת המקור נשלפת לפי ID, וה־E2E נועל כעת ש־Raw נשמר בנפרד מ־Gold. זה תיקון של שלמות נתונים, לא שינוי קוסמטי.

מסקנת המיפוי לאחר בדיקת הקבלה: צינור המעבדה וה־Trace הקנוני עובדים בפועל גם עם שירות ענן וגם עם CUDA. לא נמצאה העלאת אודיו כפולה ולא נמצאה יצירת Gold או מילון ללא פעולה מפורשת. עדיין אסור להסיק שיפור תמלול מן ההבדל 103/104/77 מילים; ללא טקסט אמת אנושי ותזמונים מאומתים אין בסיס ל־WER/CER או לקידום מודל.

מערכת הקבלה הקנונית הורחבה לשמונה שערים ועברה 8/8: TypeScript, ‏ESLint ללא שגיאות, ‏30 בדיקות Python, ‏290 בדיקות יחידה, build, ‏10 בדיקות מעבדה, Gemini באתר production ו־CUDA דרך ה־frontend המקומי על מקור ענן. בנפרד הורצה חבילת E2E הרחבה: 213 עברו, 38 נכשלו ו־5 דולגו. הכשלים הרחבים מתועדים כחוב QA לפי אשכול ואינם מוסתרים; עשרים מהם מגיעים מ־`editor.spec.ts` הישן שמחפש `contenteditable`, ותשעה מציפיות UI ישנות ב־`server-thorough.spec.ts`, בעוד חוזי ה־API באותו קובץ עברו. יש לטפל בחוב זה בסבב נפרד בלי לערבב אותו עם הוכחת הקבלה החיה של צינור התמלול.
