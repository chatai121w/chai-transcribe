# דוח בדיקות קבלה מלא למערכת התמלול

**תאריך:** 2026-09-03

**פרויקט:** `chai-transcribe`

**ענף שנבדק:** `main`

**אתר ייצור:** `https://chai-transcribe.lovable.app`

**מטרת הבדיקה:** להוכיח במסלול אמיתי שהמערכת מפעילה את הצינור הקנוני, שומרת Trace מפורט, אינה משכפלת אודיו, אינה יוצרת Gold או מונחי מילון ללא אישור, שומרת תוצאות בענן ומנקה את נתוני הבדיקה.

## 1. מסקנה מנהלית

המערכת לא נהרסה ואין צורך להתחיל מחדש. בדיקות הקבלה הוכיחו שיש מסלול מרכזי פעיל שמסוגל:

1. לטעון אודיו קיים מ־Supabase בלי להעלות אותו שוב.
2. להריץ ניסוי A/B אמיתי דרך אתר Lovable הציבורי.
3. להפעיל מנוע ענן וגם מנוע CUDA מקומי על אותו מקור.
4. לרשום Trace מלא לכל שלב ולשמור אותו בענן.
5. לשמור שתי ריצות השוואה נפרדות תחת אותו ניסוי.
6. לשמור Raw בנפרד מטקסט אמת אנושי.
7. לא לקדם חומר ל־Gold ולא לשנות מילון ללא פעולה מפורשת.
8. למחוק בסוף רק את רשומות הבדיקה שנוצרו בריצה.

כל שמונת שערי הקבלה עברו. עם זאת, אין עדיין בסיס לטעון שהאיכות השתפרה: להקלטה שנבחרה אין טקסט אמת אנושי מאומת, ולכן אין WER/CER תקפים ואין הכרעה איזה מנוע טוב יותר.

## 2. הפקודה הקנונית

נוספה פקודה אחת שמריצה את כל שערי הקבלה בסדר קבוע:

```powershell
npm run test:transcription-system -- --production --cuda
```

הפקודה כתובה ב־`scripts/run-transcription-system-qa.mjs`. היא:

- טוענת את הגדרות סביבת הייצור בלי להדפיס secrets.
- מוחקת artifacts ישנים לפני תחילת בדיקה חיה.
- מריצה כל שער בסדרה ומפסיקה אם קוד היציאה אינו אפס.
- מעבירה קוד יציאה אמיתי גם ב־Windows.
- כותבת סיכום JSON וסיכום Markdown ב־`logs/qa`.
- מסמנת בדיקה שלא התבקשה כ־skipped ולא כ־passed.
- כוללת בדוח רק ראיה שנוצרה באותה ריצה.

## 3. תוצאות הריצה הסופית

| שער | פקודה | תוצאה | זמן |
|---|---|---:|---:|
| TypeScript | `npx tsc --noEmit` | עבר | 1.3 שניות |
| ESLint | `npm run lint` | עבר: 0 שגיאות, 167 אזהרות | 6.3 שניות |
| Python server tests | `.venv\\Scripts\\python.exe -W error::ResourceWarning -m unittest discover -s server -p test*.py` | עברו 30/30 | 5.9 שניות |
| Unit tests | `npm run test:unit` | עברו 290/290 ב־58 קבצים | 16.7 שניות |
| Production build | `npm run build` | עבר, 5,187 modules | 26.7 שניות |
| Lab E2E | `npx playwright test e2e/transcription-lab.spec.ts --workers=1` | עברו 10/10 | 22.0 שניות |
| Published Gemini acceptance | `npm run test:production-lab` | עברה 1/1 | 32.5 שניות |
| Cloud audio + local CUDA | `npm run test:production-lab` | עברה 1/1 | 36.5 שניות |

**סיכום:** 8 passed, ‏0 failed, ‏0 skipped, במשך 147.9 שניות.

## 4. מקור הענן שנבדק

הבדיקה בחרה תמלול קיים עם אודיו קיים בענן:

- מזהה מקוצר: `dbf65989`.
- גודל אודיו: 1,441,376 bytes.
- תמלול מקור: 106 מילים ותזמונים.
- מצב אימות: לא Gold ולא טקסט אמת אנושי.

הבדיקה לא שינתה את רשומת המקור ולא העלתה עותק חדש. היא השתמשה ב־ID וב־audio path הקיימים, והריצה את המנועים על אותו blob חתום.

## 5. בדיקת Gemini בייצור

**Experiment ID:** `335fb0d3-4a93-43f9-9be7-bbc3199500bd`

| שדה | מנוע A | מנוע B |
|---|---|---|
| Requested model | `gemini-3.5-transcribe` | `gemini-3.5-transcribe` |
| Used model | `gemini-flash-latest` | `gemini-flash-latest` |
| Provider | `lovable` | `lovable` |
| Fallback | `personal_exhausted` | `personal_exhausted` |
| מילים | 104 | 104 |
| תזמוני מילים | 0 | 0 |

המשמעות המדויקת:

- בשני הצדדים המכסה האישית כבר הייתה אזלה.
- המערכת לא נכשלה בשקט ולא הציגה את Flash כ־3.5: ה־Trace רשם בכל צד את המודל שהתבקש, את המודל שבוצע, את הספק ואת סיבת ה־fallback.
- בדיקה זו מוכיחה התנהגות fallback תקינה ושקופה.
- היא אינה השוואת 3.5 מול 3.5, ולכן אין להשתמש בתוצאות 103/104 כהשוואת איכות בין אותם תנאים.

## 6. בדיקת CUDA על אותו מקור ענן

**Experiment ID:** `8412a027-08bf-421c-b3dc-4b0ad2261193`

| שדה | מנוע A | מנוע B |
|---|---|---|
| Requested model | `gemini-3.5-transcribe` | `ivrit-ai/whisper-large-v3-turbo-ct2` |
| Used model | `gemini-3.5-transcribe` | `ivrit-ai/whisper-large-v3-turbo-ct2` |
| Provider | `personal` | שרת CUDA מקומי |
| מילים | 103 | 77 |
| תזמוני מילים | 0 | 77 |

שרת CUDA נבדק תוך כדי הריצה:

- endpoint הבריאות החזיר `status=ok`.
- המכשיר היה `cuda` על NVIDIA GeForce RTX 5050 Laptop GPU.
- המודל הפעיל היה `ivrit-ai/whisper-large-v3-turbo-ct2`.
- בזמן התמלול `transcribe_active=true`; בסיום הוא חזר ל־false והמודל נשאר ready.
- תוצאת CUDA כללה תזמון לכל 77 המילים.

המשמעות: חוזה SSE, החזרת תמלול, word timings, שמירת ריצה ו־Trace עבדו על קובץ אמיתי. מספר מילים קטן יותר אינו הוכחת איכות טובה או רעה בלי Ground Truth.

## 7. שלמות ה־Trace

בשתי בדיקות הייצור התקבלו הנתונים הבאים:

- 34 אירועים בזיכרון המקומי.
- 34 אירועים תואמים ב־`asr_pipeline_events` בענן.
- 2 רשומות `comparison_runs` לכל ניסוי.
- `traceValid=true`.
- `traceValidationErrors=[]`.

כל עשרת שלבי הצינור הופיעו:

1. `source`
2. `configuration`
3. `upload`
4. `audio-preprocessing`
5. `transcription`
6. `knowledge`
7. `metrics`
8. `quality-gate`
9. `review`
10. `complete`

המשמעות היא שאפשר לזהות לא רק את התוצאה הסופית, אלא גם את מקור הקלט, הגדרות המנוע, עיבוד האודיו, המודל שהתבקש ובוצע, שלב הידע, המדדים, החלטת האיכות ומצב הסיום.

## 8. בדיקות נגד כפילויות וזיהום נתונים

| סיכון | בדיקה | תוצאה |
|---|---|---|
| העלאה כפולה של אודיו | נספרו פעולות upload עבור source קיים | 0 בשתי הריצות |
| יצירת Gold שקטה | Gold נספר לפני ואחרי | 0 לפני, 0 אחרי |
| שינוי מילון שקט | מונחי מילון נספרו לפני ואחרי | 3 לפני, 3 אחרי |
| Trace כפול | התאמה בין local/cloud לפי experiment ID | 34/34, ללא עודף |
| comparison run כפול | צפי לשתי ריצות A/B | בדיוק 2 |
| שאריות בדיקה | מחיקה לפי experiment ID וקריאה חוזרת | 0 events, ‏0 runs |
| מקור לא מאומת הופך לאמת | Ground Truth נבדק בטעינת המקור | נשאר ריק |

לא נמצא בבדיקות האלה מנגנון שמעלה את אותו קובץ פעמיים או מקדם טקסט לא מאומת ל־Gold.

## 9. תקלה אמיתית שהתגלתה ותוקנה

### 9.1 תיאור

במעבר מהעורך למעבדה, המשתמש יכול ללחוץ לפני ש־`useCloudTranscripts` סיים לטעון את רשימת התמלולים. הקוד מצא את ה־ID אך לא תמיד מצא את אובייקט המקור בזיכרון. במקרה כזה הוא היה עלול להשתמש בטקסט הערוך כ־`initialTranscript`, אף שהוא מיועד להיות Ground Truth.

התוצאה האפשרית הייתה ערבוב בין:

- Raw: מה שהמנוע תמלל.
- Corrected/Gold candidate: מה שהאדם תיקן.

### 9.2 תיקון

ב־`TextEditor.tsx`, הפונקציה `approveAndOpenTranscriptionLab` מבצעת כעת:

1. חיפוש ברשימה שכבר נטענה.
2. אם המקור חסר, שליפה ישירה מ־Supabase לפי `id`.
3. עצירה עם הודעה ברורה אם גם השליפה הישירה לא מצאה רשומה.
4. העברת `current.text` כ־initial transcript.
5. העברת הטקסט הערוך בלבד כ־ground truth.

בדיקת ה־E2E נכשלה לפני התיקון משום ששני הצדדים קיבלו את הטקסט המתוקן, ועברה לאחר התיקון. לכן מדובר בשחזור ותיקון מוכחים, לא בהשערה.

## 10. סינון שגיאות ניווט צפויות

נוסף מקור אמת אחד ב־`src/lib/navigationAbort.ts` עם הפונקציה `isExpectedNavigationAbort`.

היא משמשת את:

- `src/hooks/useCloudTranscripts.ts`
- `src/hooks/useCloudFolders.ts`
- `src/hooks/useTranscriptionJobs.ts`

המסווג משתיק רק fetch שבוטל בזמן ניווט/reload כאשר המסמך מוסתר. הוא אינו משתיק שגיאות רשת רגילות במסמך פעיל. נוספו שתי בדיקות יחידה שמוכיחות את שני הכיוונים: ביטול צפוי מסווג כצפוי, ושגיאה אמיתית אינה מסווגת ככזו.

בבדיקת הייצור נרשמו 4 ביטולי ניווט צפויים בריצת Gemini ו־2 בריצת CUDA. הם לא סווגו כשגיאות ענן ולא הסתירו failed request אמיתי.

## 11. הקבצים שנוספו או שונו

### מערכת בדיקות

- `e2e/transcription-lab.production.spec.ts`: בדיקת קבלה אמיתית, שמירת evidence וניקוי.
- `scripts/run-transcription-system-qa.mjs`: orchestrator קנוני לכל שערי המערכת.
- `scripts/run-playwright.mjs`: חלוקת suites והעברת exit code אמינה.
- `playwright.config.ts`: הפרדה בין smoke, full, live ו־production.
- `package.json`: פקודות `test:live`, ‏`test:production-lab`, ‏`test:transcription-system`.

### שלמות נתונים ולוגים

- `src/pages/TextEditor.tsx`: שליפת מקור לפי ID ושמירת Raw בנפרד מ־Ground Truth.
- `src/lib/navigationAbort.ts`: מסווג ביטולי ניווט קנוני.
- `src/lib/navigationAbort.test.ts`: בדיקות חיוביות ושליליות למסווג.
- `src/hooks/useCloudTranscripts.ts`: שימוש במסווג המשותף.
- `src/hooks/useCloudFolders.ts`: שימוש במסווג המשותף.
- `src/hooks/useTranscriptionJobs.ts`: שימוש במסווג המשותף.

### תיעוד

- `יומן-ביצוע-איחוד-מערכת-התמלול-2026-09-02.md`
- `מיפוי-מלא-של-כל-מסלולי-התמלול-והכפילויות-2026-09-02.md`
- מסמך זה.

## 12. מה הוכח ומה עדיין לא

### הוכח

- האתר הציבורי נגיש ומאפשר התחברות.
- המעבדה הציבורית מריצה A/B אמיתי.
- Gemini 3.5 יכול לפעול דרך הספק האישי.
- fallback עקב מכסה נרשם עם סיבה ומודל בפועל.
- CUDA מקומי מתמלל אודיו שנשלף מן הענן.
- Trace נשמר מקומית ובענן לכל שלבי הצינור.
- תוצאות comparison נשמרות ונקראות לאחר reload.
- source קיים אינו מועלה שוב.
- בדיקה אינה יוצרת Gold או מילון.
- cleanup מסיר רק את נתוני הניסוי.
- Raw ו־Ground Truth נשמרים בתפקידים נפרדים.

### לא הוכח עדיין

- שיפור WER או CER של מנוע מסוים.
- שיפור Term Recall למונחי לשון הקודש.
- איכות כללית של Gemini 3.5 מול CUDA.
- יכולת לאמן מודל חדש מנתוני הייצור הנוכחיים.
- זמינות שרת CUDA למשתמש שאין אצלו את השירות המקומי.

הסיבה המרכזית: אין עדיין מספיק Gold אנושי, מתוזמן ומגוון. Trace מלא הוא תנאי לבקרה, אך אינו מחליף אמת אנושית.

## 13. חוב בדיקות רחב

הריצה הקנונית הנוכחית ירוקה ואמינה למסלול התמלול שנבדק. חבילת E2E הרחבה של כל הריפו הורצה בנפרד, בסדרה וב־Chromium headless:

- 213 עברו.
- 38 נכשלו.
- 5 דולגו במכוון.
- משך: 21.8 דקות.

פילוח 38 הכשלים:

| קובץ/אשכול | כמות | מצב ראשוני |
|---|---:|---|
| `editor.spec.ts` | 20 | מחפש משטח `contenteditable` ישן; המוצר עבר לעריכה מלאה עם textarea ולבדיקות ממוקדות חדשות |
| `server-thorough.spec.ts` | 9 | ציפיות UI ישנות ל־badges/preload/stop; כל 13 חוזי ה־API באותו spec עברו |
| `meeting-recorder.spec.ts` | 3 | דורש triage נפרד של library/recovery UI |
| `audio-clean-lab.spec.ts` | 2 | נוסחים או שלב AI ישנים |
| ASR training, folders, mobile benchmark, studio spacing | 4 | נתיב או locator ישן; ב־studio נרשמה חסימת pointer שדורשת בדיקה נפרדת |

החבילה הרחבה כוללת גם specs ישנים ומסכים שאינם חלק מהצינור הקנוני. היא נבדקת בנפרד כדי שלא לערבב בין:

- כשל במוצר הפעיל.
- fixture ישן שלא מדמה SSE או schema נוכחי.
- בדיקה חיצונית שדורשת שירות מקומי או מדיה.
- מסלול legacy שאינו פעיל בראוטר.

אין להצהיר שכל בדיקות הריפו ירוקות: 38 הכשלים הם חוב QA מתועד. רובם אינם סותרים את בדיקות הקבלה של התמלול, אך אין למחוק או לדלג עליהם באופן גורף. יש לעבור אשכול אחר אשכול, לעדכן oracle רק כאשר המוצר החדש הוא ההתנהגות הרצויה, ולתקן מוצר כאשר השחזור עדיין רלוונטי. לעומת זאת, אין לתת לחוב בדיקות ישן לבטל את הראיות הספציפיות שכבר עברו בייצור.

## 14. המלצות לביצוע

### P0: לפני אימון נוסף

1. להמשיך לשמור Raw, ‏Corrected ו־Gold כישויות נפרדות.
2. לאמן רק על קטעים עם `qualityTier` ו־`labelSource` מזוהים.
3. לא לקדם מקור לא מאומת ל־Gold דרך בדיקת קבלה או השוואת מנועים.
4. לדרוש fingerprint קפוא ל־train ול־eval.
5. לדחות מודל אם WER או CER נסוגים, גם אם מדד אחר השתפר.

### P1: בדיקות איכות

1. לבחור כמה הקלטות ממספר דוברים וסביבות.
2. לתקן ידנית טקסט אמת ולשמור תזמוני מילים מאומתים.
3. להקפיא holdout לפי הקלטת מקור, לא לפי קטע אקראי.
4. להריץ Baseline מול Candidate על אותו audio fingerprint.
5. למדוד WER, ‏CER ו־Term Recall רק במקום שבו יש מונחי יעד.
6. לבדוק regression בנפרד לקריאה, שיעור, ארמית ושמות תורניים.

### P2: תפעול

1. להריץ את הפקודה הקנונית לפני כל Publish משמעותי.
2. לבצע Publish ב־Lovable אחרי ה־push של תיקוני ה־QA ושלמות הנתונים.
3. להריץ שוב production acceptance אחרי Publish ולוודא asset hash חדש.
4. לשמור artifacts של ריצות שנכשלו; למחוק מן הענן רק נתוני ניסוי זמניים לפי ID.
5. להוסיף audit persistence למסך התמלול הראשי אם רוצים היסטוריה מרכזית גם מחוץ למעבדה.

## 15. תנאי סגירה מומלצים לסבב הבא

הסבב הבא ייחשב סגור רק כאשר:

- commit השינויים נמצא ב־`origin/main`.
- GitHub Actions ירוק.
- Lovable פורסם מן commit הזה.
- בדיקת production Gemini עוברת שוב מול ה־bundle החדש.
- בדיקת CUDA עוברת או מסומנת במפורש כלא זמינה בסביבת הפריסה.
- מקור אחד לפחות מקבל Ground Truth אנושי מאומת.
- WER/CER מחושבים מול אותו Gold בלי leakage.
- אין שינוי במספר קובצי המקור, Gold או מילון מלבד הפעולות שאושרו במפורש.

## 16. הכרעה

יש כעת תשתית בדיקות שמסוגלת להראות בכל ריצה איזה מנוע התבקש, איזה מנוע הופעל, באיזה ספק, האם היה fallback, איזה מקור שימש, אילו שלבים רצו, כמה אירועים נשמרו והאם cleanup הצליח. זהו הבסיס הנכון להסרת כפילויות ולהחלטה על סדר שכבות לפי ראיות.

הצעד המקצועי הבא אינו להוסיף עוד כללים או לאמן מיד. יש לדחוף את תיקוני הקבלה ושלמות הנתונים, לפרסם אותם, ולאחר מכן לבנות Gold אנושי קטן אך אמין. רק אז אפשר לבצע ניסוי איכות ולהחליט אם מילון, כלל, Gemini או אימון קולי באמת שיפרו את התמלול.
