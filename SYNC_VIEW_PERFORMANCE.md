# תצוגת הסנכרון — למה היא הקפיאה ומה תוקן

מסמך לפיתוח. מתעד חקירה של קיפאון מלא בעורך הטקסט, את הסיבות שנמצאו, ואת מה
שהענף הזה מתקן.

---

## התסמין

בעורך הטקסט, תמלול ארוך נפתח ב"מצב מהיר" — `textarea` פשוט, בלי סנכרון לאודיו.
לחיצה על **"פתח סנכרון מלא"** מציגה את התצוגה המסונכרנת, ואז:

- הטקסט אכן עוקב אחרי האודיו
- אבל הדפדפן **נתקע לחלוטין**, ובמקרים חמורים כל המחשב

הסף שמפעיל את המצב המהיר נמצא ב-`TextEditor.tsx`:

```ts
const isLargeEditorSession =
  text.length > 18_000 || textWordCount > 2_500 || wordTimings.length > 2_500;
```

תמלול לדוגמה שבו נחקרה הבעיה: **5,619 מילים, 28,798 תווים, 5,619 word timings**
— שיעור מוקלט של 50 דקות.

---

## איך נמדד

הקיפאון חוסם את ה-main thread, ולכן כלי מדידה שרצים על אותו thread לא מדווחים
כלום — `setTimeout` לא נורה, `requestAnimationFrame` לא רץ. השיטה שעבדה:

1. **`PerformanceObserver` על `longtask`** — נדרך בקריאה אחת, נקרא בקריאה נפרדת
   אחריה. משימה מעל 50ms נרשמת גם אם ה-thread חסום.
2. **`MutationObserver`** במקביל — סופר כמה צמתי DOM באמת השתנו.

ההצלבה בין השניים היא שפתרה את החקירה.

### הממצא שהכריע

| מדד | ערך |
|---|---|
| זמן חסימה לעדכון מילה אחת | **14,358 ms** |
| צמתי DOM שהשתנו באותו עדכון | **29** |

עשרים ושתיים אלף אחוז של עבודה מיותרת. שלב ה-commit של React היה זעיר — **כל
הזמן נשרף בשלב ה-render**, כלומר בהרצה מחדש של פונקציית הרינדור, לפני שנגעו
ב-DOM בכלל.

זה שלל מיד את שני החשודים הראשונים (אנימציות CSS, עלות DOM) והצביע על עבודה
יקרה שרצה **פר מילה**.

---

## הסיבות שנמצאו, לפי סדר ההשפעה

### 1. קריאת `localStorage` לכל מילה, בכל פריים

בתוך לולאת הרינדור של המילים:

```ts
const wordApproved   = side === "left" && isWordApproved(wt.word);
const highlightStyle = side === "left" ? getWordHighlightStyle(wt.word) : undefined;
```

והשרשרת מאחוריהן (`personalPronunciationModel.ts`):

```
isWordApproved(word)  →  loadApproved()   →  readJSON()  →  localStorage.getItem() + JSON.parse()
getWordHighlightStyle →  loadHighlights() →  readJSON()  →  localStorage.getItem() + JSON.parse()
```

`localStorage.getItem` הוא **סינכרוני**. עם 11,238 spans (5,619 מילים × שני
פאנלים) זה מעל **אחד־עשר אלף קריאות ו-JSON.parse בפריים אחד** — ואז `.includes()`
שסורק ליניארית רשימה של עד 5,000 מילים.

**התיקון:** מטמון בנקודת החנק היחידה של המודול. כל קריאה עוברת דרך `readJSON`
וכל כתיבה דרך `writeJSON`, ולכן המטמון לא יכול לסטות ממה שמאוחסן:

```ts
const _jsonCache = new Map<string, unknown>();
// writeJSON מרענן אותו; אירוע storage (טאב אחר) מפיל אותו.
```

שלושה מקומות שינו את הערך הטעון **במקום** (`list.unshift`, `all[k] = …`,
`delete all[k]`) והפכו למעתיקים — אחרת היו מרעילים את המטמון. `isWordApproved`
עבר ל-`Set`.

**תוצאה: 14,358ms → 2,254ms.**

### 2. תפריט הקשר שלם לכל מילה

כל מילה עטפה את עצמה ב-`WordContextMenu` — קומפוננטת Radix מלאה עם תתי־תפריטים,
בורר צבעים, רשימת הצעות, ושני `useState` משלה:

```
~49 אלמנטי JSX למילה  ×  11,238 מילים  ≈  550,000 אלמנטי React בכל רינדור
```

בקצב של ~4µs לאלמנט — כ-2.2 שניות. תואם למדידה.

**התיקון:** מופע יחיד משותף במקום 11,238. `WordContextMenu` עוטף עכשיו את כל
העץ פעם אחת, `handleWordContextMenu` מזהה על איזו מילה נלחץ, והתוכן מגודר
ב-`{open && word && (…)}` כך שהוא נבנה רק בפתיחה.

**תוצאה: 2,254ms → 6-7ms.**

### 3. שער הרינדור שלא סינן כלום

`SyncAudioPlayer` מפרסם שעון ב-20Hz. `TextEditor` אמור לסנן אותו:

```ts
// לפני — 0.05s הוא בדיוק מרווח הפרסום, אז כל טיק עבר
if (idx !== lastWordIdxRef.current || elapsed >= 0.05 || …)
```

הכוונה הייתה לחסום, אבל הסף היה זהה לקצב הפרסום, ולכן **כל עשרים הטיקים בשנייה
גרמו לרינדור מלא של העורך**. מילה נמשכת בממוצע ~540ms, אז 18 מתוך 20 היו עבודה
מיותרת.

**התיקון:** רינדור כשהמילה הפעילה מתחלפת, עם רצפה של 0.25s לשעון בלבד.

### 4. `alignmentMode` — הבעיה שהענף הזה מתקן

כשפאנל נעול ומצב **"יישור 1:1"** (`mirrored-padded`) דלוק, שני הצדדים מציגים
גרסאות שונות של אותו טקסט וצריך לרפד אותם כדי ששורות תואמות ישבו זו מול זו.
זה diff, והוא רץ על **כל** רשימת השורות:

```ts
const dp: number[][] = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0));
```

ב-916 שורות: **~840,000 תאים ו-916 הקצאות נפרדות**, כולן סינכרוניות ברגע
שמדליקים את המצב.

---

## מה הענף הזה משנה

### הלוגיקה יצאה למודול משלה

`src/lib/lineAlignment.ts` — הפונקציה `buildLineAlignment(snapshot, current)`.

היא ישבה inline בתוך קומפוננטה של 2,000 שורות, וזו הסיבה שאלגוריתם ריבועי על
כל התמלול לא נתפס עד שהקפיא את הטאב. עכשיו היא חמישים שורות עם שם, עם טסטים.

### שלושה שינויים אלגוריתמיים

**גזימת קידומת וסיומת משותפות.** עריכה משאירה את הטקסט מעליה ומתחתיה זהה, אז
המטריצה מכסה רק את הקטע שבאמת שונה — בדרך כלל כמה שורות במקום 916.

```ts
let pre = 0;
while (pre < n && pre < m && A[pre] === B[pre]) pre++;
let suf = 0;
while (suf < n - pre && suf < m - pre && A[n - 1 - suf] === B[m - 1 - suf]) suf++;
```

**`Int32Array` שטוח** במקום מערך של מערכים: הקצאה אחת, ו-stride ידוע במקום
הפניה עקיפה לכל שורה בלולאה הפנימית.

**שסתום ביטחון עם anchors** — מעל `MAX_ALIGN_CELLS` (250,000) הפונקציה מאתרת
שורות ייחודיות משותפות, בוחרת מהן רצף עולה ומחלקת את העבודה למרווחים קטנים.
כך שתי עריכות רחוקות אינן גורמות לכל מה שביניהן להיראות ערוך. רק מקטע גדול
ללא anchor בטוח מסומן כולו כערוך במקום להקצות מטריצה שתקפיא את הטאב.

**חלוקת blocks משותפת** — גם התמלול הפעיל וגם `lockedSnapshotText` עוברים דרך
`chunkTranscriptTimings`. קודם הצילום הנעול הוכנס כמערך בעל שורה אחת, ולכן
היישור השווה יחידות שונות ולא יכול היה להוכיח את התנהגות המסך האמיתית.

### בנוסף — מסלול מהיר לזהות

מיד אחרי נעילה שני הצדדים זהים, וזה המצב הנפוץ. במקרה הזה אין diff בכלל:

```ts
if (n === m && A.every((row, i) => row === B[i])) { … }
```

---

## אימות

### טסטים

`src/lib/lineAlignment.test.ts` ו-`src/lib/transcriptBlocks.test.ts` — 10 טסטים.
הם נועלים את מה שהתצוגה המרופדת באמת
תלויה בו:

1. שני הפאנלים יוצאים באותו גובה
2. כל פאנל עדיין משחזר בדיוק את הטקסט המקורי שלו
3. שורה נשארת בלי סימון **רק** כששני הצדדים באמת מסכימים

**המימוש הישן שמור בקובץ הטסט כאורקל.** הוא פשוט מספיק כדי לסמוך עליו בקריאה,
ולכן כל אי־הסכמה בינו לבין המימוש החדש היא באג בחדש. ההשוואה רצה על 2,000
עריכות שורה אקראיות, והמימוש החדש **חייב למצוא לפחות כמה שהישן מצא**.

שני מקרים נעולים בכוונה:

- **תקרה של תא אחד** עדיין מיישרת 100 שורות סביב אחת שנערכה. נכשל אם הגזימה
  לא קורית — כלומר הטסט מוכיח שהיא באמת קורית.
- **900 שורות שונות לגמרי** לוקחות את מסלול המילוט; אותו קלט עם תקרה מורמת
  לוקח את המסלול המדויק ונשאר תקין.
- **שתי עריכות רחוקות בתוך 900 שורות** משמרות 898 התאמות גם כשהמקטע שביניהן
  גדול מתקרת המטריצה.
- **5,619 מילים** מתחלקות לאותם blocks בצילום הנעול ובצד הפעיל.

בנוסף, `SyncMirrorLayout.alignment.test.tsx` מרנדר את הקומפוננטה עם 5,619 מילים,
נועל צד, מבצע שתי עריכות רחוקות ומוודא שרק ה-blocks שנפגעו מסומנים.

```
npx vitest run src/lib/lineAlignment.test.ts src/lib/transcriptBlocks.test.ts → 10 passed
npx vitest run src/components/SyncMirrorLayout.alignment.test.tsx             → 1 passed
```

### מדידות

| תרחיש | לפני | אחרי |
|---|---|---|
| 900 שורות, 3 עריכות | 25.3 ms | **2.9 ms** |
| נכתב מחדש לגמרי | 6.9 ms | **0.8 ms** |
| 2,000 עריכות אקראיות | תקין | תקין, אף פעם לא פחות התאמות |

### מדידות התצוגה בכללותה (מצטבר, כולל התיקונים שכבר ב-main)

| מדד | לפני | אחרי |
|---|---|---|
| רינדור בודד | 2,254 ms | **6-7 ms** |
| main thread חסום בזמן ניגון | 100% | **2%** |
| long tasks | 2,000-3,800 ms כל אחד | אחד, 125 ms |
| אודיו מול שעון קיר | 16 ש' אודיו, אפס פריימים | 6.0 ש' ב-6.6 — זמן אמת |

---

## מכשור

`src/lib/syncPerfTrace.ts` — כבוי כברירת מחדל. להפעלה בקונסול:

```js
localStorage.setItem('debug_sync_perf', '1'); location.reload();
```

לכיבוי:

```js
localStorage.removeItem('debug_sync_perf'); location.reload();
```

מה שמדווח:

| שורה | משמעות |
|---|---|
| `▶ full sync requested` | גודל העבודה: מילים, תווים, spans צפויים |
| `📊 SyncMirrorLayout` | סיכום מתגלגל: רינדורים לשנייה, ממוצע, גרוע ביותר |
| `🕐 player clock` | `rendersPerSec` צריך להתלכד עם `wordChangesPerSec`, **לא** עם `ticksPerSec` |
| `⇉ padded alignment` | ממדי ה-diff וכמה שורות הותאמו |
| `🧱 main thread blocked` | כל משימה מעל 50ms, עם השלב שהיה בתהליך |

---

## מה לא נבדק

נגינת אודיו אמיתי לאורך שיעור שלם בתוך session מחובר לא נכללת בטסט האוטומטי.
מסלול הרינדור הכבד עצמו כן מכוסה ב-component test עם 5,619 מילים, נעילה ושתי
עריכות רחוקות. מדידות ניגון מצטברות בטבלה לעיל נשארות מדידות ידניות מהחקירה,
ולא assertion של CI.

---

## קבצים

| קובץ | שינוי |
|---|---|
| `src/lib/lineAlignment.ts` | חדש — האלגוריתם |
| `src/lib/lineAlignment.test.ts` | 8 טסטים + אורקל |
| `src/lib/transcriptBlocks.ts` | מקור אמת יחיד לחלוקת תמלול ו-snapshot ל-blocks |
| `src/lib/transcriptBlocks.test.ts` | 2 טסטים למסלול 5,619 מילים |
| `src/components/SyncMirrorLayout.alignment.test.tsx` | טסט component לנעילה ועריכות רחוקות |
| `src/components/SyncMirrorLayout.tsx` | שימוש באלגוריתם ובחלוקת blocks המשותפת |
